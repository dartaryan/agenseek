# מדריך התאמה אישית של BMAD-METHOD

> **מדריך מקיף** להתאמה אישית של קונפיגורציה, אג'נטים, וורקפלואים ופלט ב-BMAD

---

## 📋 תוכן עניינים

### [חלק 1: קבצי קונפיגורציה](#חלק-1-קבצי-קונפיגורציה)
- [היררכיית קבצי Config](#היררכיית-קבצי-config)
- [מיקום קבצי הקונפיגורציה](#מיקום-קבצי-הקונפיגורציה)
- [config.yaml - מבנה ושדות](#configyaml---מבנה-ושדות)
- [שינויים נפוצים בקונפיגורציה](#שינויים-נפוצים-בקונפיגורציה)
- [קונפיגורציה ספציפית למודול](#קונפיגורציה-ספציפית-למודול)

### [חלק 2: התאמה אישית של אג'נטים](#חלק-2-התאמה-אישית-של-אגנטים)
- [מערכת Customize Files](#מערכת-customize-files)
- [שינוי שמות אג'נטים](#שינוי-שמות-אגנטים)
- [התאמת אישיות אג'נטים](#התאמת-אישיות-אגנטים)
- [שינוי שפת אג'נטים](#שינוי-שפת-אגנטים)
- [הוספת זיכרונות (Memories)](#הוספת-זיכרונות-memories)
- [הוספת תפריטים מותאמים אישית](#הוספת-תפריטים-מותאמים-אישית)
- [דוגמאות התאמה אישית מלאות](#דוגמאות-התאמה-אישית-מלאות)
- [החלת שינויים](#החלת-שינויים)

### [חלק 3: התאמה אישית של וורקפלואים](#חלק-3-התאמה-אישית-של-וורקפלואים)
- [הבנת מבנה Workflow](#הבנת-מבנה-workflow)
- [יצירת וורקפלוא מותאם אישית - יסודות](#יצירת-וורקפלוא-מותאם-אישית---יסודות)
- [שינוי וורקפלואים קיימים](#שינוי-וורקפלואים-קיימים)
- [מתי להתאים אישית vs לקבל ברירת מחדל](#מתי-להתאים-אישית-vs-לקבל-ברירת-מחדל)
- [Workflow Templates והרחבות](#workflow-templates-והרחבות)

### [חלק 4: התאמה אישית של פלט](#חלק-4-התאמה-אישית-של-פלט)
- [קונפיגורציית תיקיית פלט](#קונפיגורציית-תיקיית-פלט)
- [תבניות מסמכים](#תבניות-מסמכים)
- [כללי שמות (Naming Conventions)](#כללי-שמות-naming-conventions)
- [Document Sharding](#document-sharding)
- [התאמת פורמט וסגנון](#התאמת-פורמט-וסגנון)

---

## חלק 1: קבצי קונפיגורציה

### היררכיית קבצי Config

BMAD משתמש במערכת היררכית של קבצי קונפיגורציה. הבנת ההיררכיה הזו חיונית לביצוע שינויים נכונים.

#### רמות הקונפיגורציה

**רמה 1: Core Config (הכי כללי)**
```
bmad/core/config.yaml
```
זהו קובץ הקונפיגורציה הראשי המשפיע על כל המערכת.

**רמה 2: Module Configs (ספציפי למודול)**
```
bmad/bmm/config.yaml
bmad/bmb/config.yaml
bmad/cis/config.yaml
```
כל מודול יכול לדרוס הגדרות מ-core או להוסיף הגדרות משלו.

**רמה 3: Agent Customizations (ספציפי לאג'נט)**
```
bmad/_cfg/agents/*.customize.yaml
```
התאמות אישיות לאג'נטים בודדים.

#### סדר עדיפויות

כאשר יש קונפליקט, סדר העדיפויות הוא:

```
Agent Customize > Module Config > Core Config
```

**דוגמה:**

אם ב-`core/config.yaml` יש:
```yaml
communication_language: English
```

וב-`bmm/config.yaml` יש:
```yaml
communication_language: Hebrew
```

אז אג'נטים מ-BMM ידברו **עברית**, בעוד אג'נטים מ-Core ידברו **אנגלית**.

---

### מיקום קבצי הקונפיגורציה

#### מבנה תיקיות מלא

```
bmad/
├── core/
│   ├── config.yaml              # ⭐ קונפיג ראשי של המערכת
│   ├── agents/
│   ├── tasks/
│   └── workflows/
├── bmm/
│   ├── config.yaml              # קונפיג של BMad Method
│   ├── agents/
│   └── workflows/
├── bmb/
│   ├── config.yaml              # קונפיג של BMad Builder
│   ├── agents/
│   └── workflows/
├── cis/
│   ├── config.yaml              # קונפיג של Creative Intelligence
│   ├── agents/
│   └── workflows/
└── _cfg/                         # ⭐ תיקיית התאמות אישיות
    ├── agents/                   # קבצי customize לאג'נטים
    │   ├── core-bmad-master.customize.yaml
    │   ├── bmm-dev.customize.yaml
    │   ├── bmm-architect.customize.yaml
    │   └── ... (19 קבצים נוספים)
    ├── manifest.yaml             # רשימת כל הרכיבים
    ├── agent-manifest.csv
    ├── workflow-manifest.csv
    └── task-manifest.csv
```

#### קבצים שכדאי להכיר

**קבצי Config חובה:**
- `bmad/core/config.yaml` - **חייב להכיר!** הגדרות בסיסיות
- `bmad/{module}/config.yaml` - לכל מודול שהתקנתם

**קבצי Manifest (קריאה בלבד):**
- `bmad/_cfg/agent-manifest.csv` - רשימת כל האג'נטים
- `bmad/_cfg/workflow-manifest.csv` - רשימת כל הוורקפלואים
- `bmad/_cfg/task-manifest.csv` - רשימת כל הטאסקים

**קבצי Customize (להתאמה אישית):**
- `bmad/_cfg/agents/*.customize.yaml` - התאמת אג'נטים בודדים

---

### config.yaml - מבנה ושדות

#### מבנה בסיסי של config.yaml

כל קובץ `config.yaml` במערכת BMAD מכיל לפחות את השדות הבאים:

```yaml
# CORE Module Configuration
# Generated by BMAD installer
# Version: 6.0.0-alpha.5
# Date: 2025-11-04T16:59:31.777Z

user_name: BMad
communication_language: English
document_output_language: English
output_folder: '{project-root}/docs'
```

#### שדות חובה - הסבר מפורט

##### 1. user_name

**מה זה:** השם שהאג'נטים יפנו אליכם בו.

**ערכים אפשריים:** כל שם (טקסט חופשי)

**דוגמאות:**
```yaml
user_name: David
user_name: Sarah Chen
user_name: Team Alpha
user_name: דוד כהן
```

**השפעה:**
```
עם user_name: David
→ "Hi David! Let's review your PRD..."

עם user_name: Team Alpha
→ "Hi Team Alpha! Let's review your PRD..."
```

**טיפ:** אם אתם עובדים בצוות, שקלו שם צוות במקום שם אישי.

##### 2. communication_language

**מה זה:** השפה שבה האג'נטים מדברים איתכם בשיחות.

**ערכים נפוצים:**
```yaml
communication_language: English
communication_language: Hebrew
communication_language: Spanish
communication_language: French
communication_language: German
communication_language: Japanese
```

**השפעה:**
- **English:** "Let's start by analyzing the requirements..."
- **Hebrew:** "בואו נתחיל בניתוח הדרישות..."
- **Spanish:** "Empecemos analizando los requisitos..."

**חשוב:** זה משפיע **רק על השיחות**, לא על המסמכים שנוצרים!

##### 3. document_output_language

**מה זה:** השפה שבה מסמכים (PRD, Architecture, Stories) נכתבים.

**למה זה נפרד מ-communication_language?**

לעיתים קרובות אתם רוצים:
- **לדבר** עם האג'נט בשפה אחת (למשל, עברית)
- אבל ש**המסמכים** יהיו בשפה אחרת (למשל, אנגלית לצוות בינלאומי)

**דוגמאות לתרחישים:**

**תרחיש 1: צוות ישראלי בלבד**
```yaml
communication_language: Hebrew
document_output_language: Hebrew
```

**תרחיש 2: מפתח ישראלי, צוות בינלאומי**
```yaml
communication_language: Hebrew    # דובר עברית עם האג'נט
document_output_language: English # מסמכים באנגלית
```

**תרחיש 3: צוות בינלאומי**
```yaml
communication_language: English
document_output_language: English
```

##### 4. output_folder

**מה זה:** התיקייה שבה BMAD שומר את כל המסמכים שנוצרים.

**ערך ברירת מחדל:**
```yaml
output_folder: '{project-root}/docs'
```

**Placeholders זמינים:**
- `{project-root}` - שורש הפרויקט (מומלץ!)
- `{user-home}` - תיקיית home של המשתמש
- `{module}` - שם המודול הנוכחי

**דוגמאות:**
```yaml
# תיקייה יחסית לפרויקט (מומלץ)
output_folder: '{project-root}/docs'
output_folder: '{project-root}/documentation'

# תיקייה מוחלטת
output_folder: 'C:\Projects\MyProject\docs'
output_folder: '/Users/david/Documents/MyProject'

# תיקייה בענן
output_folder: '/Users/david/OneDrive/Projects/MyProject/docs'
```

**שיקולים:**
- ✅ השתמשו ב-`{project-root}` לעבודת צוות (יחסי)
- ✅ תיקייה בתוך הפרויקט טובה ל-Git
- ❌ נתיבים מוחלטים יוצרים בעיות בצוות

---

### שינויים נפוצים בקונפיגורציה

#### שינוי #1: שם משתמש

**מתי:** אחרי התקנה, רוצים לשנות את השם.

**איך:**

```bash
# פתחו את הקובץ
nano bmad/core/config.yaml

# שנו את השורה
user_name: YourNewName

# שמרו (Ctrl+O) ויצאו (Ctrl+X)
```

**או - בעריכה גרפית:**
1. פתחו `bmad/core/config.yaml` ב-IDE
2. מצאו `user_name:`
3. שנו את הערך
4. שמרו

**אין צורך ב-rebuild!** השינוי נכנס לתוקף מיד.

---

#### שינוי #2: שפת תקשורת

**תרחיש:** התקנתם באנגלית אבל רוצים לדבר עברית עם האג'נטים.

**פתרון:**

```yaml
# bmad/core/config.yaml
communication_language: Hebrew
```

**תוצאה:** מעכשיו כל האג'נטים ידברו איתכם בעברית.

**זכרו:** זה לא משפיע על שפת המסמכים (זה `document_output_language`)!

---

#### שינוי #3: שפת מסמכים

**תרחיש:** רוצים שכל המסמכים יהיו בעברית.

```yaml
document_output_language: Hebrew
```

**תוצאה:**
- PRD נכתב בעברית
- Technical Architecture בעברית
- User Stories בעברית
- כל פלט של BMAD בעברית

---

#### שינוי #4: תיקיית פלט

**תרחיש 1: שינוי שם התיקייה**

```yaml
# מ:
output_folder: '{project-root}/docs'

# ל:
output_folder: '{project-root}/documentation'
```

**תרחיש 2: שמירה מחוץ לפרויקט**

```yaml
output_folder: '/Users/david/Dropbox/ProjectDocs'
```

**⚠️ אזהרה:** אם אתם עובדים בצוות, **כולם צריכים אותו output_folder יחסי**!

**טוב:**
```yaml
output_folder: '{project-root}/docs'  # כולם יכולים להשתמש בזה
```

**רע:**
```yaml
output_folder: 'C:\Users\David\MyDocs'  # רק אצלכם יעבוד!
```

---

#### שינוי #5: שילוב שינויים

אפשר לשנות מספר שדות בבת אחת:

```yaml
# bmad/core/config.yaml
user_name: דוד כהן
communication_language: Hebrew
document_output_language: English
output_folder: '{project-root}/תיעוד'
```

**תוצאה:**
- האג'נט קורא לכם "דוד כהן"
- מדבר איתכם בעברית
- כותב מסמכים באנגלית
- שומר ב-`תיעוד/` במקום `docs/`

---

### קונפיגורציה ספציפית למודול

כל מודול יכול להוסיף שדות קונפיגורציה משלו.

#### BMM (BMad Method)

```yaml
# bmad/bmm/config.yaml
user_name: David
communication_language: English
document_output_language: English
output_folder: '{project-root}/docs'

# הגדרות ייחודיות ל-BMM
enable_game_development: true
default_planning_track: bmad_method
agile_sprint_length: 2
story_id_prefix: "STORY"
```

**שדות ייחודיים:**

| שדה | ערכים אפשריים | תיאור |
|-----|---------------|--------|
| `enable_game_development` | `true` / `false` | הצגת אג'נטים ווורקפלואים של פיתוח משחקים |
| `default_planning_track` | `quick_flow`, `bmad_method`, `enterprise` | מסלול ברירת מחדל ב-workflow-init |
| `agile_sprint_length` | `1`, `2`, `3`, `4` | אורך ספרינט בשבועות |
| `story_id_prefix` | טקסט | prefix לסטוריז (למשל: "US", "FEAT", "TASK") |

**דוגמה מלאה:**

```yaml
# bmad/bmm/config.yaml
user_name: Sarah
communication_language: Hebrew
document_output_language: English
output_folder: '{project-root}/docs'

enable_game_development: false     # אין צורך ב-game dev
default_planning_track: quick_flow # מסלול מהיר
agile_sprint_length: 2             # ספרינטים של שבועיים
story_id_prefix: "US"              # US-001, US-002, ...
```

---

#### CIS (Creative Intelligence Suite)

```yaml
# bmad/cis/config.yaml
user_name: David
communication_language: English
document_output_language: English
output_folder: '{project-root}/docs'

# הגדרות ייחודיות ל-CIS
default_facilitation_style: energetic
brainstorming_techniques_limit: 10
ideation_time_box: 30
capture_method: structured
```

**שדות ייחודיים:**

| שדה | ערכים אפשריים | תיאור |
|-----|---------------|--------|
| `default_facilitation_style` | `energetic`, `calm`, `analytical`, `playful` | סגנון הובלת סשנים |
| `brainstorming_techniques_limit` | מספר (1-20) | כמה טכניקות להציע בכל סשן |
| `ideation_time_box` | מספר (דקות) | זמן ברירת מחדל לסשן חשיבה |
| `capture_method` | `structured`, `free-form`, `visual` | איך לתעד רעיונות |

---

#### BMB (BMad Builder)

```yaml
# bmad/bmb/config.yaml
user_name: David
communication_language: English
document_output_language: English
output_folder: '{project-root}/docs'

# הגדרות ייחודיות ל-BMB
custom_agents_folder: '{project-root}/my-agents'
workflow_template_style: detailed
auto_register_agents: true
```

**שדות ייחודיים:**

| שדה | ערכים אפשריים | תיאור |
|-----|---------------|--------|
| `custom_agents_folder` | נתיב | היכן לשמור אג'נטים מותאמים אישית |
| `workflow_template_style` | `minimal`, `standard`, `detailed` | רמת פירוט בתבניות |
| `auto_register_agents` | `true` / `false` | האם לרשום אג'נטים חדשים אוטומטית |

---

## חלק 2: התאמה אישית של אג'נטים

### מערכת Customize Files

אחת מהתכונות החזקות ביותר של BMAD v6 היא **מערכת קבצי ה-customize**.

#### הרעיון המרכזי

**בעיה ב-v4:**
- רציתם לשנות אג'נט? היה צריך לערוך את הקובץ המקורי
- עדכון BMAD? דרס את השינויים שלכם! 😱

**הפתרון ב-v6: Customize Files**
- קובץ `.customize.yaml` לכל אג'נט
- עורכים רק את ה-customize, לא את המקור
- עדכונים לא דורסים את ההתאמות שלכם! ✅

#### מיקום קבצי Customize

```
bmad/_cfg/agents/
├── core-bmad-master.customize.yaml
├── bmm-analyst.customize.yaml
├── bmm-architect.customize.yaml
├── bmm-dev.customize.yaml
├── bmm-game-architect.customize.yaml
├── bmm-game-designer.customize.yaml
├── bmm-game-dev.customize.yaml
├── bmm-paige.customize.yaml
├── bmm-pm.customize.yaml
├── bmm-sm.customize.yaml
├── bmm-tea.customize.yaml
├── bmm-tech-writer.customize.yaml
├── bmm-ux-designer.customize.yaml
├── cis-brainstorming-coach.customize.yaml
├── cis-creative-problem-solver.customize.yaml
├── cis-design-thinking-coach.customize.yaml
├── cis-innovation-strategist.customize.yaml
├── cis-storyteller.customize.yaml
└── bmb-bmad-builder.customize.yaml
```

**יש קובץ customize לכל אג'נט במערכת!**

---

### שינוי שמות אג'נטים

#### למה לשנות שם?

- אתם לא אוהבים את השם המקורי
- רוצים שם בעברית
- רוצים שם שמתאים לצוות/תרבות הארגונית

#### איך לשנות

**קובץ:** `bmad/_cfg/agents/bmm-dev.customize.yaml`

```yaml
# Override agent name
agent:
  metadata:
    name: "CodeMaster Pro"
```

**תוצאה:**
- במקום "Amelia (Dev)" תקבלו "CodeMaster Pro"
- האג'נט יזדהה בשם החדש בכל אינטראקציה

**דוגמאות נוספות:**

**שם בעברית:**
```yaml
agent:
  metadata:
    name: "אמיר המפתח"
```

**שם ארגוני:**
```yaml
agent:
  metadata:
    name: "Senior Full-Stack Developer"
```

**שם צוותי:**
```yaml
agent:
  metadata:
    name: "Backend Team Lead"
```

---

### התאמת אישיות אג'נטים

זוהי אחת ההתאמות החזקות ביותר - אתם יכולים לשנות **לגמרי** את האישיות של אג'נט.

#### מבנה Persona

```yaml
persona:
  role: ""                    # תפקיד
  identity: ""                # זהות
  communication_style: ""     # סגנון תקשורת
  principles: []              # עקרונות
```

#### דוגמה 1: Dev Agent חברותי

```yaml
# bmad/_cfg/agents/bmm-dev.customize.yaml
persona:
  role: "Senior Full-Stack Developer + Mentor"
  identity: "Experienced developer with 15 years in the field, passionate about teaching and helping others grow. Specializes in React, Node.js, and cloud architecture."
  communication_style: "Friendly, patient, encouraging. Uses emojis to make code less intimidating 😊. Always explains the 'why' before the 'how'."
  principles:
    - "Write clean, maintainable code"
    - "Test-driven development is not optional"
    - "Code reviews are learning opportunities"
    - "Always consider performance implications"
    - "Documentation is part of the code"
```

**תוצאה:**
האג'נט יהיה יותר חברותי, יסביר הרבה יותר, וישתמש באמוג'ים.

---

#### דוגמה 2: Architect Agent טכני מאוד

```yaml
# bmad/_cfg/agents/bmm-architect.customize.yaml
persona:
  role: "Principal Software Architect + Systems Designer"
  identity: "Deep technical expert focused on scalability, performance, and system design patterns. 20 years of experience building distributed systems."
  communication_style: "Direct, technical, data-driven. Provides detailed technical justifications. Uses diagrams and architecture patterns."
  principles:
    - "Design for scale from day one"
    - "Always consider failure modes"
    - "Measure everything, optimize what matters"
    - "Loose coupling, high cohesion"
    - "Security and privacy by design"
    - "Documentation is architecture"
```

---

#### דוגמה 3: PM Agent דוגמתי לסטארטאפ

```yaml
# bmad/_cfg/agents/bmm-pm.customize.yaml
persona:
  role: "Startup Product Manager + Growth Hacker"
  identity: "Fast-moving PM focused on rapid iteration and user feedback. Believes in shipping fast and learning faster."
  communication_style: "Energetic, concise, action-oriented. Uses startup lingo. Focuses on impact and metrics."
  principles:
    - "Ship fast, learn faster"
    - "Data beats opinions"
    - "Focus on the 20% that delivers 80% of value"
    - "Talk to users every week"
    - "MVPs should be embarrassingly simple"
```

---

### שינוי שפת אג'נטים

אתם יכולים לגרום לאג'נט לדבר בשפה ספציפית, בלי לשנות את ה-`communication_language` הגלובלי.

#### דוגמה: Dev Agent בעברית

```yaml
# bmad/_cfg/agents/bmm-dev.customize.yaml
persona:
  role: "מפתח Full-Stack בכיר"
  identity: "מפתח מנוסה עם 10 שנות ניסיון בפיתוח אפליקציות ווב. מתמחה ב-React, Node.js, ו-TypeScript."
  communication_style: "ישיר, ברור, וידידותי. משתמש בדוגמאות קוד רבות. תמיד מסביר את הרציונל מאחורי ההחלטות."
  principles:
    - "קוד נקי וקריא"
    - "תיעוד הוא חלק מהקוד"
    - "בדיקות אוטומטיות חובה"
    - "Code review הוא הזדמנות ללמוד"
```

**תוצאה:** האג'נט ידבר איתכם **בעברית**, גם אם ה-`communication_language` הוא English.

---

### הוספת זיכרונות (Memories)

זיכרונות הם מידע שהאג'נט **"זוכר"** בכל שיחה. זה מאוד חזק!

#### מה זה Memories?

Memories הם הקשר קבוע שהאג'נט לוקח בחשבון בכל פעם שאתם פונים אליו.

**דוגמאות טובות ל-Memories:**
- ✅ טכנולוגיות שהפרויקט משתמש בהן
- ✅ coding standards של הצוות
- ✅ העדפות אישיות שלכם
- ✅ מידע על הפרויקט הנוכחי

**דוגמאות רעות:**
- ❌ מידע שמשתנה כל הזמן
- ❌ מידע רגיש (passwords, keys)
- ❌ פרטים ספציפיים לסטורי בודד

#### דוגמה 1: Dev Agent עם Stack Context

```yaml
# bmad/_cfg/agents/bmm-dev.customize.yaml
memories:
  - "Project uses React 18 with TypeScript"
  - "Backend is Node.js with Express and PostgreSQL"
  - "Team follows Airbnb JavaScript Style Guide"
  - "All components must be functional (no class components)"
  - "Use async/await instead of .then() promises"
  - "Material-UI (MUI) is the component library"
  - "State management with Redux Toolkit"
  - "Always write unit tests with Jest and React Testing Library"
```

**תוצאה:**
כשתבקשו מהאג'נט לכתוב קוד, הוא אוטומטית:
- ישתמש ב-React functional components
- יכתוב ב-TypeScript
- ישתמש ב-async/await
- יציע MUI components
- יכתוב tests

---

#### דוגמה 2: Architect עם Architecture Decisions

```yaml
# bmad/_cfg/agents/bmm-architect.customize.yaml
memories:
  - "Microservices architecture with event-driven communication"
  - "Using Kubernetes for orchestration"
  - "PostgreSQL for transactional data, Redis for caching"
  - "RabbitMQ for message queue"
  - "REST APIs with OpenAPI specification"
  - "Authentication via OAuth 2.0 with JWT tokens"
  - "All services must have health check endpoints"
  - "Monitoring with Prometheus and Grafana"
```

---

#### דוגמה 3: PM עם Product Context

```yaml
# bmad/_cfg/agents/bmm-pm.customize.yaml
memories:
  - "Product is a SaaS B2B platform for team collaboration"
  - "Target market: small to medium businesses (10-200 employees)"
  - "Key competitors: Slack, Microsoft Teams, Asana"
  - "Main value proposition: unified workspace for all team tools"
  - "Current phase: MVP launched, 500 active users"
  - "Business model: Freemium with paid team plans"
  - "Key metrics: DAU, retention rate, conversion to paid"
```

---

### הוספת תפריטים מותאמים אישית

אתם יכולים להוסיף פריטי תפריט מותאמים אישית לכל אג'נט.

#### מתי להשתמש

- ✅ יש לכם workflow מותאם אישית שהאג'נט צריך להריץ
- ✅ רוצים shortcut למשימה נפוצה
- ✅ צריך פעולה ספציפית לצוות/ארגון

#### מבנה בסיסי

```yaml
menu:
  - trigger: my-workflow
    workflow: "{project-root}/custom/my.yaml"
    description: My custom workflow
```

#### דוגמה 1: Dev Agent עם Code Review Workflow

```yaml
# bmad/_cfg/agents/bmm-dev.customize.yaml
menu:
  - trigger: code-review
    workflow: "{project-root}/custom-workflows/code-review.yaml"
    description: "Run comprehensive code review"
  - trigger: refactor-check
    workflow: "{project-root}/custom-workflows/refactor-analysis.yaml"
    description: "Analyze code for refactoring opportunities"
  - trigger: security-scan
    workflow: "{project-root}/custom-workflows/security-check.yaml"
    description: "Check for security vulnerabilities"
```

**איך להשתמש:**

כשתטענו את ה-Dev agent ותכתבו:
```
*code-review
```

האג'נט יריץ את ה-workflow המותאם אישית שלכם!

---

#### דוגמה 2: PM Agent עם Product Workflows

```yaml
# bmad/_cfg/agents/bmm-pm.customize.yaml
menu:
  - trigger: user-feedback
    workflow: "{project-root}/workflows/analyze-feedback.yaml"
    description: "Analyze user feedback and extract insights"
  - trigger: roadmap-update
    workflow: "{project-root}/workflows/update-roadmap.yaml"
    description: "Update product roadmap based on priorities"
  - trigger: stakeholder-report
    workflow: "{project-root}/workflows/generate-report.yaml"
    description: "Generate stakeholder progress report"
```

---

### דוגמאות התאמה אישית מלאות

#### דוגמה מלאה #1: Dev Agent מותאם לצוות React

```yaml
# bmad/_cfg/agents/bmm-dev.customize.yaml

# שם בעברית
agent:
  metadata:
    name: "עמית - מפתח React"

# אישיות מותאמת
persona:
  role: "React Developer Expert + TypeScript Specialist"
  identity: "Experienced React developer focused on modern patterns, hooks, and performance optimization. Strong advocate for clean code and testing."
  communication_style: "Friendly and practical. Provides code examples. Explains trade-offs between different approaches."
  principles:
    - "Components should be small and focused"
    - "Custom hooks for reusable logic"
    - "Performance matters - use React.memo wisely"
    - "Accessibility is not optional"
    - "TypeScript strict mode always"

# זיכרונות טכניים
memories:
  - "Project: E-commerce platform with React 18 + TypeScript"
  - "Stack: React, TypeScript, Redux Toolkit, React Query"
  - "UI Library: Chakra UI"
  - "Testing: Jest + React Testing Library"
  - "Build: Vite for faster development"
  - "Code style: ESLint + Prettier with Airbnb config"
  - "Always use functional components with hooks"
  - "Prefer composition over inheritance"
  - "Use React Query for server state"
  - "Redux only for global UI state"

# תפריט מותאם
menu:
  - trigger: component-audit
    workflow: "{project-root}/workflows/audit-components.yaml"
    description: "Audit React components for best practices"
  - trigger: performance-check
    workflow: "{project-root}/workflows/check-performance.yaml"
    description: "Analyze component rendering performance"
```

---

#### דוגמה מלאה #2: Architect Agent לסביבה ארגונית

```yaml
# bmad/_cfg/agents/bmm-architect.customize.yaml

agent:
  metadata:
    name: "System Architect - Enterprise Grade"

persona:
  role: "Enterprise Solution Architect + Cloud Infrastructure Expert"
  identity: "25 years of experience designing large-scale distributed systems. Expert in AWS, microservices, and event-driven architectures. Strong focus on security, compliance, and operational excellence."
  communication_style: "Formal, detailed, documentation-oriented. Always provides architectural diagrams. Considers compliance and security in every decision."
  principles:
    - "Design for failure - everything will fail eventually"
    - "Security and compliance are non-negotiable"
    - "Document all architectural decisions (ADRs)"
    - "Measure twice, cut once - validate before building"
    - "Operational excellence from day one"
    - "Cost optimization is part of architecture"

memories:
  - "Company: Fortune 500 financial services"
  - "Cloud: AWS (mandatory)"
  - "Compliance: SOC 2, PCI DSS, GDPR"
  - "Architecture style: Event-driven microservices"
  - "Service mesh: Istio on EKS"
  - "Databases: Aurora PostgreSQL (transactional), DynamoDB (operational)"
  - "Message queue: Amazon SQS/SNS"
  - "API Gateway: AWS API Gateway with Lambda authorizers"
  - "Observability: DataDog (approved vendor)"
  - "IaC: Terraform (required for all infrastructure)"
  - "Security: All data encrypted at rest and in transit"
  - "DR/BC: Multi-region active-active setup"

menu:
  - trigger: adr
    workflow: "{project-root}/workflows/architecture-decision-record.yaml"
    description: "Create Architecture Decision Record"
  - trigger: security-review
    workflow: "{project-root}/workflows/security-architecture-review.yaml"
    description: "Comprehensive security architecture review"
  - trigger: cost-analysis
    workflow: "{project-root}/workflows/cost-architecture-analysis.yaml"
    description: "Analyze architecture costs and optimization opportunities"
```

---

### החלת שינויים

אחרי שעריכתם קובץ customize, צריך להחיל את השינויים.

#### אופציה 1: Build ספציפי (מהיר)

```bash
# בנו מחדש רק את האג'נט שערכתם
npx bmad-method build bmm-dev

# או עם שם מלא
npx bmad-method build bmad/bmm/agents/dev
```

**זמן:** 5-10 שניות  
**מתי להשתמש:** כששינית אג'נט אחד בלבד

---

#### אופציה 2: Build כללי

```bash
# בנו מחדש את כל האג'נטים
npx bmad-method build
```

**זמן:** 30-60 שניות  
**מתי להשתמש:** כששינית מספר אג'נטים

---

#### אופציה 3: Reinstall (המלצה לשינויים גדולים)

```bash
npx bmad-method@alpha install
```

בחרו **"Quick Update"** כשתתבקשו.

**זמן:** 1-2 דקות  
**מתי להשתמש:** שינויים מרובים או אחרי עדכון BMAD

---

#### אימות השינויים

אחרי build, טענו את האג'נט ב-IDE ובדקו:

**Claude Code:**
```
/bmad:bmm:agents:dev
```

**Cursor:**
```
@bmad/bmm/agents/dev
```

האג'נט צריך להגיב עם השם/אישיות החדשים!

---

## חלק 3: התאמה אישית של וורקפלואים

### הבנת מבנה Workflow

לפני שניצור וורקפלואים מותאמים, חשוב להבין איך הם בנויים.

#### מבנה workflow.yaml

```yaml
# Workflow Configuration
name: "My Workflow"
description: "What this workflow does"
version: "1.0.0"

# Steps to execute
steps:
  - id: step-1
    name: "First Step"
    agent: "bmm/dev"
    prompt: "Do something"
    
  - id: step-2
    name: "Second Step"
    agent: "bmm/architect"
    prompt: "Review the output from step-1"
    depends_on: [step-1]
```

#### רכיבי Workflow

**1. Metadata:**
- `name`: שם הוורקפלוא
- `description`: תיאור קצר
- `version`: גרסה (לניהול שינויים)

**2. Steps:**
- `id`: מזהה ייחודי
- `name`: שם תיאורי
- `agent`: איזה אג'נט מבצע את הצעד
- `prompt`: מה לבצע
- `depends_on`: תלויות (אופציונלי)

---

### יצירת וורקפלוא מותאם אישית - יסודות

#### מתי ליצור workflow מותאם אישית?

**תרחישים טובים:**
- ✅ יש לכם תהליך חוזר ספציפי לארגון
- ✅ רוצים לשלב מספר אג'נטים באופן ספציפי
- ✅ צריך automation למשימה מורכבת

**אל תיצרו workflow אם:**
- ❌ אפשר להשתמש בworkflow קיים
- ❌ זו משימה חד-פעמית
- ❌ מספיק אינטראקציה ידנית עם אג'נט אחד

#### צעדים ליצירת Workflow בסיסי

**צעד 1: צרו קובץ YAML**

```bash
mkdir -p custom-workflows
touch custom-workflows/my-workflow.yaml
```

**צעד 2: הוסיפו metadata**

```yaml
name: "My Custom Workflow"
description: "Description of what this workflow does"
version: "1.0.0"
```

**צעד 3: הוסיפו steps**

```yaml
steps:
  - id: analyze
    name: "Analyze Requirements"
    agent: "bmm/analyst"
    prompt: "Analyze the user's requirements and create a summary"
    
  - id: design
    name: "Design Solution"
    agent: "bmm/architect"
    prompt: "Based on the analysis from step 'analyze', design a technical solution"
    depends_on: [analyze]
    
  - id: implement
    name: "Create Implementation Plan"
    agent: "bmm/dev"
    prompt: "Based on the design from step 'design', create an implementation plan"
    depends_on: [design]
```

---

#### דוגמה מלאה: Code Review Workflow

```yaml
# custom-workflows/code-review.yaml
name: "Comprehensive Code Review"
description: "Multi-stage code review process with different perspectives"
version: "1.0.0"

steps:
  - id: security-check
    name: "Security Review"
    agent: "bmm/architect"
    prompt: |
      Review the code for security vulnerabilities:
      - SQL injection risks
      - XSS vulnerabilities
      - Authentication/authorization issues
      - Sensitive data exposure
      - Input validation
      
      Provide a security assessment report.
  
  - id: code-quality
    name: "Code Quality Review"
    agent: "bmm/dev"
    prompt: |
      Review the code for quality issues:
      - Code readability and maintainability
      - DRY principle violations
      - Proper error handling
      - Code complexity
      - Best practices adherence
      
      Provide a code quality report.
  
  - id: performance
    name: "Performance Review"
    agent: "bmm/architect"
    prompt: |
      Review the code for performance issues:
      - Database query optimization
      - API efficiency
      - Memory usage
      - Caching opportunities
      - Scalability concerns
      
      Provide a performance assessment.
    depends_on: [security-check, code-quality]
  
  - id: testing
    name: "Testing Review"
    agent: "bmm/tea"
    prompt: |
      Review test coverage and quality:
      - Unit test coverage
      - Integration test scenarios
      - Edge cases
      - Test quality and maintainability
      
      Provide testing recommendations.
    depends_on: [code-quality]
  
  - id: summary
    name: "Summary Report"
    agent: "bmm/pm"
    prompt: |
      Based on all reviews (security, quality, performance, testing),
      create a comprehensive summary report with:
      - Critical issues (must fix)
      - Important issues (should fix)
      - Nice-to-have improvements
      - Overall assessment
      - Recommended action plan
    depends_on: [security-check, code-quality, performance, testing]
```

---

### שינוי וורקפלואים קיימים

לפעמים אתם רוצים לשנות workflow קיים, לא ליצור חדש.

#### אסטרטגיה 1: Copy & Modify (מומלץ)

**שלב 1: העתיקו את הוורקפלוא המקורי**

```bash
# העתיקו workflow קיים
cp bmad/bmm/workflows/prd/workflow.yaml custom-workflows/my-prd.yaml
```

**שלב 2: ערכו לפי הצורך**

```yaml
# custom-workflows/my-prd.yaml
name: "PRD - Company Standard"
description: "PRD workflow customized for our company standards"
version: "1.0.0-custom"

# עכשיו שנו את ה-steps...
```

**שלב 3: השתמשו בגרסה המותאמת**

במקום:
```
/bmad:bmm:workflows:prd
```

הריצו:
```
Load custom-workflows/my-prd.yaml
```

---

#### אסטרטגיה 2: Override בagent customize

אם אתם רוצים שאג'נט ספציפי תמיד ישתמש בworkflow מותאם:

```yaml
# bmad/_cfg/agents/bmm-pm.customize.yaml
menu:
  - trigger: prd
    workflow: "{project-root}/custom-workflows/my-prd.yaml"
    description: "Create PRD (Company Standard)"
```

עכשיו כשתכתבו `*prd` ב-PM agent, הוא ישתמש בגרסה המותאמת!

---

### מתי להתאים אישית vs לקבל ברירת מחדל

#### השתמשו בברירת מחדל אם:

- ✅ הworkflow מתאים לצרכים שלכם
- ✅ אתם מתחילים עם BMAD
- ✅ אין לכם תהליכים ייחודיים
- ✅ רוצים לקבל עדכונים מBMAD

**יתרונות:**
- עובד מהקופסה
- מקבלים שיפורים בעדכונים
- תיעוד מלא זמין

---

#### התאימו אישית אם:

- ✅ יש לכם תהליך ארגוני ספציפי
- ✅ צריכים integration עם כלים פנימיים
- ✅ דרישות compliance מיוחדות
- ✅ צריכים פלט בפורמט ספציפי

**זכרו:**
- ⚠️ תצטרכו לתחזק את הworkflows בעצמכם
- ⚠️ לא תקבלו עדכונים אוטומטית
- ⚠️ צריך לתעד את השינויים

---

### Workflow Templates והרחבות

#### Workflow Templates נפוצים

BMAD מספק templates לworkflows נפוצים:

**1. Sequential Workflow (צעדים ברצף)**
```yaml
steps:
  - id: step1
    agent: "..."
  - id: step2
    depends_on: [step1]
  - id: step3
    depends_on: [step2]
```

**2. Parallel Workflow (צעדים במקביל)**
```yaml
steps:
  - id: frontend
    agent: "bmm/dev"
  - id: backend
    agent: "bmm/dev"
  - id: database
    agent: "bmm/architect"
  - id: integration
    depends_on: [frontend, backend, database]
```

**3. Conditional Workflow (עם תנאים)**
```yaml
steps:
  - id: analysis
    agent: "bmm/analyst"
  - id: simple-solution
    agent: "bmm/dev"
    condition: "complexity == 'low'"
    depends_on: [analysis]
  - id: complex-solution
    agent: "bmm/architect"
    condition: "complexity == 'high'"
    depends_on: [analysis]
```

---

## חלק 4: התאמה אישית של פלט

### קונפיגורציית תיקיית פלט

#### שינוי תיקיית פלט בסיסית

ראינו זאת בחלק 1, אבל הנה סיכום מלא:

```yaml
# bmad/core/config.yaml
output_folder: '{project-root}/docs'
```

**אפשרויות נפוצות:**

```yaml
# תיקייה ברירת מחדל
output_folder: '{project-root}/docs'

# תיקייה עם שם אחר
output_folder: '{project-root}/documentation'

# תיקייה בעברית
output_folder: '{project-root}/תיעוד'

# תיקייה מחוץ לפרויקט
output_folder: '/Users/david/ProjectDocs'

# תיקייה בענן
output_folder: '/Users/david/OneDrive/MyProject/docs'
```

---

#### מבנה תיקיות משנה

BMAD יוצר אוטומטית תיקיות משנה בתוך `output_folder`:

```
docs/
├── prd/                    # Product Requirements Documents
├── architecture/           # Technical Architecture
├── stories/                # User Stories
├── test-plans/            # Test Plans
├── game-design/           # Game Design Documents (אם BMM game dev מופעל)
├── sprint-plans/          # Sprint Planning
└── documentation/         # Technical Documentation
```

**זה אוטומטי - אין צורך להגדיר!**

---

#### קונפיגורציה מתקדמת לפי סוג מסמך

אם אתם רוצים **שליטה מלאה** על איפה כל סוג מסמך נשמר:

```yaml
# bmad/bmm/config.yaml
output_folder: '{project-root}/docs'

# התאמה אישית לפי סוג מסמך (אופציונלי)
output_paths:
  prd: '{project-root}/product/requirements'
  architecture: '{project-root}/tech/architecture'
  stories: '{project-root}/agile/stories'
  test_plans: '{project-root}/qa/test-plans'
```

---

### תבניות מסמכים

BMAD משתמש בתבניות פנימיות לכל סוג מסמך. אתם יכולים לדרוס אותן.

#### מיקום תבניות

תבניות ברירת מחדל:
```
bmad/bmm/workflows/{workflow-name}/template.md
```

תבניות מותאמות אישית:
```
custom-templates/
├── prd-template.md
├── architecture-template.md
└── story-template.md
```

---

#### דוגמה: תבנית PRD מותאמת אישית

**צעד 1: צרו תבנית**

```markdown
<!-- custom-templates/prd-template.md -->
# Product Requirements Document

**Product:** {{product_name}}
**Version:** {{version}}
**Date:** {{date}}
**Author:** {{author}}

---

## 🎯 Executive Summary
{{executive_summary}}

## 🌟 Vision and Goals
{{vision_goals}}

## 👥 Target Users
{{target_users}}

## ✨ Features and Requirements

### Core Features
{{core_features}}

### Nice-to-Have Features
{{nice_to_have}}

## 🚫 Out of Scope
{{out_of_scope}}

## 📊 Success Metrics
{{success_metrics}}

## 📅 Timeline
{{timeline}}

---

**Approved by:** _______________  
**Date:** _______________
```

**צעד 2: הגדירו שימוש בתבנית**

```yaml
# bmad/bmm/config.yaml
templates:
  prd: '{project-root}/custom-templates/prd-template.md'
```

---

### כללי שמות (Naming Conventions)

BMAD משתמש בכללי שמות לקבצים שנוצרים. אתם יכולים להתאים אותם.

#### ברירות מחדל

```
docs/
├── prd/
│   └── product-requirements.md
├── stories/
│   ├── story-001-user-auth.md
│   ├── story-002-dashboard.md
│   └── story-003-api-integration.md
├── architecture/
│   └── technical-architecture.md
```

#### קונפיגורציה מותאמת אישית

```yaml
# bmad/bmm/config.yaml
naming_conventions:
  story_prefix: "STORY"           # STORY-001, STORY-002
  story_format: "{prefix}-{id}-{slug}"
  architecture_name: "tech-arch"   # tech-arch.md במקום technical-architecture.md
```

**דוגמאות לפורמטים:**

```yaml
# פורמט 1: עם prefix ארגוני
naming_conventions:
  story_prefix: "US"
  story_format: "{prefix}-{id}-{slug}"
# → US-001-user-authentication.md

# פורמט 2: רק מספרים
naming_conventions:
  story_prefix: ""
  story_format: "{id:04d}-{slug}"
# → 0001-user-authentication.md

# פורמט 3: עם תאריך
naming_conventions:
  story_format: "{date}-{id}-{slug}"
# → 2024-11-05-001-user-authentication.md
```

---

### Document Sharding

BMAD תומך ב-**document sharding** - חלוקת מסמכים גדולים לקבצים קטנים יותר.

#### מה זה Document Sharding?

במקום מסמך אחד ענק:
```
docs/prd/product-requirements.md (5000 שורות)
```

מחלקים אותו ל-shards:
```
docs/prd/
├── _master.md                    # מסמך ראשי
└── _shards/
    ├── 01-overview.md
    ├── 02-target-users.md
    ├── 03-features.md
    ├── 04-requirements.md
    └── 05-success-metrics.md
```

#### יתרונות

- ✅ **קל יותר לעריכה** - עורכים רק את החלק הרלוונטי
- ✅ **Git friendly** - conflicts פחותים
- ✅ **טעינה מהירה** - טוענים רק מה שצריך
- ✅ **ניווט טוב יותר** - מוצאים מידע מהר

#### הפעלת Sharding

```yaml
# bmad/bmm/config.yaml
document_sharding:
  enabled: true
  max_lines_per_shard: 500        # מקסימום שורות בכל shard
  shard_by: "section"              # חלוקה לפי sections
  master_file_name: "_master.md"
  shards_folder: "_shards"
```

**אופציות חלוקה:**

| ערך | תיאור |
|-----|--------|
| `section` | חלוקה לפי כותרות H2 (##) |
| `lines` | חלוקה אחרי X שורות |
| `size` | חלוקה לפי גודל קובץ (KB) |
| `manual` | אתם קובעים איפה לחלק |

---

#### מבנה Master File

```markdown
<!-- docs/prd/_master.md -->
# Product Requirements Document

{{shard:01-overview}}
{{shard:02-target-users}}
{{shard:03-features}}
{{shard:04-requirements}}
{{shard:05-success-metrics}}

---
*This document is sharded for easier maintenance.*
```

#### Shards

```markdown
<!-- docs/prd/_shards/01-overview.md -->
## Executive Summary

[תוכן...]

## Vision and Goals

[תוכן...]
```

```markdown
<!-- docs/prd/_shards/02-target-users.md -->
## Target Users

[תוכן...]

### User Personas

[תוכן...]
```

---

#### כיצד לעבוד עם Sharded Documents

**עריכה:**
1. פתחו את ה-shard הספציפי
2. ערכו רק אותו
3. המסמך המלא מתעדכן אוטומטית

**קריאה מלאה:**
- פתחו את `_master.md`
- BMAD יאסוף את כל ה-shards אוטומטית

**Git:**
- Commit את `_master.md` **וגם** את כל ה-shards
- כך כולם יכולים לראות את השינויים

---

### התאמת פורמט וסגנון

#### שינוי סגנון Markdown

BMAD משתמש ב-Markdown, אבל אתם יכולים להתאים את הסגנון.

**קונפיגורציה:**

```yaml
# bmad/bmm/config.yaml
markdown_style:
  heading_style: "atx"              # # Heading (or "setext")
  list_style: "dash"                # - item (or "asterisk" or "number")
  emphasis_style: "asterisk"        # *italic* (or "underscore")
  code_fence: "backtick"            # ``` (or "tilde")
  line_width: 120                   # רוחב שורה מקסימלי
```

**דוגמה - סגנון קפדני:**

```yaml
markdown_style:
  heading_style: "atx"
  list_style: "dash"
  emphasis_style: "asterisk"
  code_fence: "backtick"
  line_width: 80                    # שורות קצרות יותר
  enforce_blank_lines: true         # שורה ריקה בין פסקאות
```

---

#### הוספת אלמנטים מותאמים אישית

אתם יכולים להוסיף frontmatter או אלמנטים מיוחדים:

```yaml
# bmad/bmm/config.yaml
document_frontmatter:
  enabled: true
  fields:
    - author
    - date
    - version
    - status
    - reviewers
```

**תוצאה - כל מסמך יכלול:**

```markdown
---
author: David Cohen
date: 2024-11-05
version: 1.0.0
status: Draft
reviewers: 
  - Sarah (PM)
  - Mike (Architect)
---

# Product Requirements Document
...
```

---

#### סגנון כותרות וStructure

```yaml
# bmad/bmm/config.yaml
document_structure:
  use_emoji_headers: true           # 🎯 Headers עם אמוג'י
  table_of_contents: true           # TOC אוטומטי
  numbered_sections: false          # 1., 1.1, 1.1.1
  include_metadata: true            # תאריך, גרסה בכותרת
```

**עם אמוג'י:**
```markdown
# 📋 Product Requirements Document

## 🎯 Executive Summary
## 👥 Target Users
## ✨ Features
## 📊 Success Metrics
```

**בלי אמוג'י:**
```markdown
# Product Requirements Document

## Executive Summary
## Target Users
## Features
## Success Metrics
```

---

#### התאמה לפי שפה

אם אתם כותבים בעברית, שקלו:

```yaml
# bmad/core/config.yaml
document_output_language: Hebrew

# bmad/bmm/config.yaml
rtl_support: true                   # תמיכה ב-Right-to-Left
hebrew_date_format: true            # תאריכים בעברית
localized_headers: true             # כותרות בעברית
```

**תוצאה:**
```markdown
---
מחבר: דוד כהן
תאריך: 5 בנובמבר 2024
גרסה: 1.0.0
---

# מסמך דרישות מוצר

## תקציר מנהלים
...
```

---

## 🎓 סיכום והמלצות

### מה למדנו

✅ **קבצי קונפיגורציה** - איפה הם, מה הם עושים, איך לשנות  
✅ **התאמת אג'נטים** - שמות, אישיות, זיכרונות, תפריטים  
✅ **התאמת וורקפלואים** - יצירה, שינוי, מתי להשתמש  
✅ **התאמת פלט** - תיקיות, תבניות, שמות, sharding  

### אסטרטגיית התאמה מומלצת

#### שלב 1: התחילו עם ברירות מחדל (שבוע ראשון)

```yaml
# bmad/core/config.yaml
user_name: YourName
communication_language: Hebrew    # או English
document_output_language: English
output_folder: '{project-root}/docs'
```

**אל תשנו עוד כלום!** למדו איך BMAD עובד out-of-the-box.

---

#### שלב 2: התאימו אג'נטים (שבוע שני)

אחרי שלמדתם את האג'נטים, התחילו להתאים:

```yaml
# bmad/_cfg/agents/bmm-dev.customize.yaml
memories:
  - "Project uses [your stack]"
  - "Team follows [your style guide]"
  - "[Other project-specific info]"
```

**רק זיכרונות בשלב הזה!** לא אישיות, לא תפריטים.

---

#### שלב 3: הוסיפו workflows מותאמים (חודש ראשון)

אם גיליתם תהליכים חוזרים:

```bash
mkdir custom-workflows
# צרו workflows ספציפיים לצרכים שלכם
```

**התחילו מ-copy של workflow קיים**, שנו בהדרגה.

---

#### שלב 4: התאימו פלט (חודש שני)

כשאתם יודעים מה עובד:

```yaml
# bmad/bmm/config.yaml
document_sharding:
  enabled: true
naming_conventions:
  story_prefix: "YOUR-PREFIX"
templates:
  prd: '{project-root}/custom-templates/prd.md'
```

---

### טעויות נפוצות להימנע מהן

❌ **לא לעשות:**
1. לשנות הכל ביום הראשון
2. לערוך קבצים מקוריים (רק customize files!)
3. לשכוח לעשות build אחרי שינויים
4. להשתמש ב-paths מוחלטים בצוות
5. לשנות workflow בלי להבין את המקורי

✅ **כן לעשות:**
1. להתחיל קטן - שינויים הדרגתיים
2. להשתמש ב-customize files
3. לתעד שינויים (comments ב-YAML)
4. להשתמש ב-{project-root} placeholders
5. לשמור גיבויים לפני שינויים גדולים

---

### מקרי שימוש נפוצים

#### מקרה 1: Startup זריז

```yaml
# התאמה מינימלית
communication_language: English
document_output_language: English
enable_game_development: false
default_planning_track: quick_flow

# אג'נטים עם זיכרונות
memories:
  - "Move fast, ship often"
  - "MVP mindset"
  - "Tech stack: [your stack]"
```

---

#### מקרה 2: Enterprise גדול

```yaml
# התאמה מלאה
communication_language: English
document_output_language: English
default_planning_track: enterprise

# קונפיגורציה מפורטת
document_sharding:
  enabled: true
templates:
  prd: '{project-root}/company-templates/prd.md'
naming_conventions:
  story_prefix: "JIRA"

# אג'נטים עם compliance
memories:
  - "SOC 2 compliance required"
  - "All data encrypted"
  - "Architecture Decision Records mandatory"
  - "Security review for every PR"
```

---

#### מקרה 3: צוות ישראלי

```yaml
# תקשורת בעברית, מסמכים באנגלית
communication_language: Hebrew
document_output_language: English
output_folder: '{project-root}/docs'

# אג'נטים בעברית
persona:
  role: "מפתח בכיר"
  communication_style: "ישיר וברור בעברית"
memories:
  - "הצוות עובד בעברית"
  - "המסמכים באנגלית ללקוחות"
```

---

### כלים ומשאבים

#### קבצים חשובים לסימניה

```
📁 bmad/core/config.yaml               - קונפיג ראשי
📁 bmad/bmm/config.yaml                - קונפיג BMM
📁 bmad/_cfg/agents/*.customize.yaml   - התאמות אג'נטים
📁 bmad/_cfg/manifest.yaml             - מפת המערכת
```

#### פקודות שימושיות

```bash
# בניית אג'נט אחד
npx bmad-method build bmm-dev

# בניית כל האג'נטים
npx bmad-method build

# עדכון מהיר
npx bmad-method@alpha install
# → בחרו "Quick Update"

# בדיקת קונפיג
cat bmad/core/config.yaml
cat bmad/_cfg/agents/bmm-dev.customize.yaml
```

---

### מה הלאה?

**מדריכים קשורים:**
- [התקנה והגדרה מתקדמת](./04-התקנה-והגדרה-מתקדמת.md) - קונפיגורציה מתקדמת
- [שיטות עבודה מומלצות](./05-שיטות-עבודה-מומלצות.md) - Best practices
- [FAQ - פיתוח](./06-faq-part-3a-development.md) - שאלות נפוצות
- [מילון מונחים](./01-מילון-מונחים-מלא.md) - כל המושגים

**משאבים נוספים:**
- [BMB Module](../../src/modules/bmb/README.md) - יצירת אג'נטים מתקדמים
- [Document Sharding Guide](../../docs/document-sharding-guide.md) - מדריך מפורט
- [Discord Community](https://discord.gg/gk8jAdXWmj) - קהילת BMAD

---

### טיפים לסיום

1. **התחילו קטן** - אל תשנו הכל בבת אחת
2. **תעדו שינויים** - הוסיפו comments ב-YAML files
3. **גבו לפני שינויים גדולים** - copy של customize files
4. **נסו ב-development קודם** - לא straight to production
5. **שתפו עם הצוות** - ודאו שכולם מבינים את ההתאמות
6. **עדכנו בזהירות** - שינויים עשויים להשפיע על workflows קיימים

---

## 📚 נספחים

### נספח א': מפתח שדות Config

| שדה | סוג | ברירת מחדל | תיאור |
|-----|-----|-----------|--------|
| `user_name` | string | `"BMad"` | שם המשתמש |
| `communication_language` | string | `"English"` | שפת תקשורת |
| `document_output_language` | string | `"English"` | שפת מסמכים |
| `output_folder` | path | `'{project-root}/docs'` | תיקיית פלט |
| `enable_game_development` | boolean | `true` | הפעלת game dev |
| `default_planning_track` | string | `"bmad_method"` | מסלול תכנון |

### נספח ב': Placeholders זמינים

| Placeholder | דוגמה | תיאור |
|------------|--------|--------|
| `{project-root}` | `/path/to/project` | שורש הפרויקט |
| `{user-home}` | `/Users/david` | תיקיית home |
| `{module}` | `bmm` | שם המודול |
| `{date}` | `2024-11-05` | תאריך נוכחי |
| `{time}` | `14:30:00` | שעה נוכחית |

### נספח ג': דוגמאות לקבצי Customize מלאים

זמין ב:
```
bmad/_cfg/agents/*.customize.yaml
```

כל קובץ מכיל comments והסברים מפורטים!

---

<div dir="rtl" align="center">

**🎉 מזל טוב! השלמתם את המדריך להתאמה אישית!**

עכשיו אתם יכולים להתאים את BMAD בדיוק לצרכים שלכם.  
התחילו קטן, התנסו, והדרגתית בנו את הסביבה המושלמת! 🚀

---

*נוצר עבור BMAD-METHOD v6 | עדכון אחרון: נובמבר 2024*

[🏠 חזרה לדף הבית](./README.md) | [📚 כל המדריכים](./README.md#-מדריכים-זמינים)

</div>