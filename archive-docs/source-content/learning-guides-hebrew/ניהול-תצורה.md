# ניהול תצורה ב-BMAD-METHOD

> **מדריך מקיף** לניהול קונפיגורציה של BMAD בצוותים וסביבות מרובות

---

## 📋 תוכן עניינים

1. [מבוא](#מבוא)
2. [קבצי קונפיגורציה במבנה BMAD](#קבצי-קונפיגורציה-במבנה-bmad)
3. [הגדרות צוות](#הגדרות-צוות)
4. [הגדרות ספציפיות לסביבה](#הגדרות-ספציפיות-לסביבה)
5. [ניהול גרסאות (Version Control)](#ניהול-גרסאות-version-control)
6. [גיבוי ושחזור קונפיגורציה](#גיבוי-ושחזור-קונפיגורציה)
7. [תרחישים נפוצים](#תרחישים-נפוצים)
8. [פתרון בעיות תצורה](#פתרון-בעיות-תצורה)
9. [Best Practices](#best-practices)
10. [שאלות נפוצות](#שאלות-נפוצות)
11. [משאבים נוספים](#משאבים-נוספים)
12. [צ'קליסט](#צקליסט-ניהול-תצורה)

---

## מבוא

### למה ניהול תצורה חשוב?

ניהול תצורה נכון ב-BMAD הוא קריטי להצלחת הפרויקט והצוות. כאשר מספר מפתחים עובדים על אותו פרויקט, או כאשר אתם עובדים על מספר סביבות (פיתוח, בדיקות, ייצור), הבדלים בקונפיגורציה יכולים לגרום לבעיות, אי-עקביות ובזבוז זמן.

**מדוע זה חשוב:**
- **עקביות בצוות:** כל חברי הצוות עובדים עם אותן הגדרות
- **נייד בין מחשבים:** מעבר בין מחשבים או הצטרפות חבר צוות חדש הופכים פשוטים
- **שליטה בשינויים:** מעקב אחר מי שינה מה ומתי
- **גיבוי ושחזור:** יכולת לחזור לקונפיגורציה קודמת אם משהו השתבש

**הבדלים בין משתמש יחיד לצוות:**

| היבט | משתמש יחיד | עבודת צוות |
|------|-----------|-----------|
| הגדרות אישיות | חופשי לשנות | צריך תיאום |
| שפת פלט | לפי בחירה | צריך להיות אחיד |
| נתיבי קבצים | יכול להיות מוחלט | חייב להיות יחסי |
| גיבוי | אופציונלי | חובה |
| Version Control | מומלץ | הכרחי |

במדריך זה נלמד כיצד לנהל תצורה בצורה מקצועית, להימנע מבעיות נפוצות, ולהבטיח עבודה חלקה של כל הצוות.

---

## קבצי קונפיגורציה במבנה BMAD

### מבנה קבצי התצורה

BMAD v6 משתמש במבנה מודולרי שבו לכל מודול יש קובץ קונפיגורציה משלו.

**מיקום קבצי Config:**

```
bmad/
├── core/
│   └── config.yaml          # הגדרות ליבה (חובה)
├── bmm/
│   └── config.yaml          # הגדרות BMad Method
├── bmb/
│   └── config.yaml          # הגדרות BMad Builder
├── cis/
│   └── config.yaml          # הגדרות Creative Intelligence
└── _cfg/
    ├── agents/              # התאמות אג'נטים
    │   ├── *.customize.yaml
    │   └── ...
    ├── agent-manifest.csv
    ├── workflow-manifest.csv
    └── manifest.yaml
```

### קובץ Core Config (חובה)

זהו קובץ הקונפיגורציה המרכזי שחייב להיות קיים:

```yaml
# bmad/core/config.yaml
user_name: BMad
communication_language: English
document_output_language: English
output_folder: '{project-root}/docs'
```

**שדות חובה:**

| שדה | תיאור | דוגמאות |
|-----|-------|----------|
| `user_name` | שם המשתמש (אישי) | David, Sarah, Team-A |
| `communication_language` | שפת תקשורת עם אג'נטים | English, Hebrew, Spanish |
| `document_output_language` | שפת מסמכים | English, Hebrew |
| `output_folder` | תיקיית פלט | `{project-root}/docs` |

### קבצי Customize (התאמות צוות)

```
bmad/_cfg/agents/
├── core-bmad-master.customize.yaml
├── bmm-dev.customize.yaml
├── bmm-architect.customize.yaml
└── ...
```

קבצים אלה מאפשרים התאמה אישית של אג'נטים ללא שינוי הקבצים המקוריים. זהו המקום **המומלץ** להתאמות ברמת הצוות.

### מה כל קובץ שולט עליו

**core/config.yaml:**
- הגדרות גלובליות
- שפת ממשק וממשק
- נתיבי פלט בסיסיים

**bmm/config.yaml:**
- הגדרות פיתוח תוכנה
- אפשרויות פיתוח משחקים
- העדפות תהליך

**cis/config.yaml:**
- הגדרות סיעור מוחות
- סגנון פסיליטציה
- העדפות יצירתיות

**_cfg/ (directory):**
- התאמות אג'נטים (customize files)
- Manifests של אג'נטים ווורקפלואים
- קונפיגורציה ברמת הצוות

---

## הגדרות צוות

### עקרון הבסיס: הפרדה בין אישי לצוות

**הגדרות אישיות (Personal):**
- `user_name` - כל אחד משתמש בשמו
- `communication_language` - כל אחד בוחר שפה
- נתיבים מוחלטים למחשב

**הגדרות צוות (Team):**
- `document_output_language` - **חייב** להיות אחיד
- `output_folder` - **חייב** להיות יחסי
- `*.customize.yaml` - התאמות אג'נטים משותפות
- הגדרות ספציפיות למודול

### סטנדרטיזציה של הגדרות צוות

#### שלב 1: הגדרת קונבנציות

צרו מסמך "BMAD Team Standards" שמגדיר:

**קונבנציית שפה:**
```yaml
# Standard for all team members
document_output_language: English  # או Hebrew לצוותים ישראליים
```

**קונבנציית תיקיות:**
```yaml
# Standard folder structure
output_folder: '{project-root}/docs'
```

**קונבנציית מודולים:**
```yaml
# BMM settings for software projects
enable_game_development: false
default_planning_track: bmad_method
```

#### שלב 2: יצירת Template Configuration

צרו קובץ `team-config-template.yaml` במאגר:

```yaml
# TEAM CONFIGURATION TEMPLATE
# Copy values from here to your personal config files

# Core settings (shared)
document_output_language: English
output_folder: '{project-root}/docs'

# BMM settings (shared)
enable_game_development: false
default_planning_track: bmad_method
user_skill_level: intermediate

# Personal settings (customize these)
user_name: "YOUR_NAME_HERE"
communication_language: "YOUR_LANGUAGE_HERE"
```

#### שלב 3: סנכרון הגדרות קיימות

כאשר צוות מתחיל לעבוד עם BMAD:

**אופציה 1: Onboarding Script**

```bash
#!/bin/bash
# setup-bmad-team.sh

echo "Setting up BMAD with team standards..."

# קריאת שם מהמשתמש
read -p "Enter your name: " USER_NAME
read -p "Enter your preferred language (English/Hebrew): " USER_LANG

# עדכון core config עם הגדרות צוות + אישיות
cat > bmad/core/config.yaml << EOF
user_name: $USER_NAME
communication_language: $USER_LANG
document_output_language: English
output_folder: '{project-root}/docs'
EOF

echo "✅ BMAD configured with team standards!"
```

**אופציה 2: Git Hook**

צרו `.git/hooks/post-checkout`:

```bash
#!/bin/bash
# בדיקה שההגדרות עדכניות

if [ -f "team-config-template.yaml" ]; then
  echo "⚠️  Team config updated. Please review your personal settings."
fi
```

### שיתוף קונפיגורציה בצוות

#### אסטרטגיה 1: Git + Team Template

**מה להכניס ל-Git:**
```
✅ team-config-template.yaml        # Template משותף
✅ bmad/_cfg/                        # התאמות אג'נטים
✅ docs/                             # מסמכים שנוצרו
❌ bmad/core/config.yaml            # אישי
❌ bmad/*/config.yaml               # אישי
```

**בעת onboarding של חבר צוות חדש:**
1. `git clone` הפרויקט
2. `npx bmad-method@alpha install`
3. העתק ערכים מ-`team-config-template.yaml`
4. עדכן `user_name` ו-`communication_language` אישיים

#### אסטרטגיה 2: Shared Config + Local Overrides

צרו קובץ `bmad-team.config.yaml` במאגר:

```yaml
# bmad-team.config.yaml - SHARED TEAM SETTINGS
document_output_language: English
output_folder: '{project-root}/docs'
enable_game_development: false
```

כל מפתח יוצר `bmad-local.config.yaml` (לא ב-Git):

```yaml
# bmad-local.config.yaml - PERSONAL OVERRIDES
user_name: David
communication_language: Hebrew
```

#### אסטרטגיה 3: Environment Variables (מתקדם)

השתמשו במשתני סביבה להגדרות רגישות:

```bash
# .env (לא ב-Git!)
BMAD_USER_NAME=David
BMAD_COMM_LANG=Hebrew
```

```yaml
# config.yaml
user_name: ${BMAD_USER_NAME}
communication_language: ${BMAD_COMM_LANG}
```

### תקשורת ותיעוד

**צרו מסמך "BMAD Team Guidelines":**

```markdown
# BMAD Configuration Guidelines

## Required Settings (Team Standard)
- document_output_language: English
- output_folder: {project-root}/docs

## Personal Settings (Your Choice)
- user_name: Your name
- communication_language: Your preference

## How to Update
1. Pull latest from main
2. Run: npm install
3. Check team-config-template.yaml
4. Update your personal config if needed

## Who to Ask
- Config issues: @tech-lead
- Git issues: @devops-lead
```

---

## הגדרות ספציפיות לסביבה

### Dev vs Staging vs Production

במקרים רבים אתם צריכים הגדרות שונות לסביבות שונות.

#### דוגמה: סביבות מרובות

**Development:**
```yaml
# bmad/core/config-dev.yaml
output_folder: '{project-root}/docs-dev'
document_output_language: Hebrew  # קל יותר לפיתוח
verbose_logging: true
```

**Staging:**
```yaml
# bmad/core/config-staging.yaml
output_folder: '{project-root}/docs-staging'
document_output_language: English
verbose_logging: true
```

**Production:**
```yaml
# bmad/core/config-prod.yaml
output_folder: '{project-root}/docs'
document_output_language: English
verbose_logging: false
```

#### שימוש בקבצי סביבה

**אופציה 1: Symlink**

```bash
# Development
ln -sf config-dev.yaml bmad/core/config.yaml

# Staging
ln -sf config-staging.yaml bmad/core/config.yaml

# Production
ln -sf config-prod.yaml bmad/core/config.yaml
```

**אופציה 2: Environment Variable**

```bash
export BMAD_ENV=staging
```

```yaml
# bmad/core/config.yaml
output_folder: '{project-root}/docs-${BMAD_ENV}'
```

**אופציה 3: Branch-Specific Config**

```bash
# .gitignore
bmad/core/config.yaml

# .git/hooks/post-checkout
#!/bin/bash
BRANCH=$(git branch --show-current)
if [ "$BRANCH" = "main" ]; then
  cp config-prod.yaml bmad/core/config.yaml
elif [ "$BRANCH" = "staging" ]; then
  cp config-staging.yaml bmad/core/config.yaml
else
  cp config-dev.yaml bmad/core/config.yaml
fi
```

### התאמות לפי מפתח

כל מפתח יכול לשמור על הגדרות מקומיות מבלי להשפיע על הצוות.

#### Local Overrides Pattern

**1. הגדרות משותפות (ב-Git):**
```yaml
# bmad/core/config.shared.yaml
document_output_language: English
output_folder: '{project-root}/docs'
```

**2. הגדרות מקומיות (לא ב-Git):**
```yaml
# bmad/core/config.local.yaml
user_name: David
communication_language: Hebrew
output_folder: '/Users/david/Projects/MyProject/docs'
```

**3. Merge Script:**
```bash
#!/bin/bash
# merge-config.sh
# מאחד config.shared + config.local → config.yaml

cat bmad/core/config.shared.yaml > bmad/core/config.yaml
cat bmad/core/config.local.yaml >> bmad/core/config.yaml
```

### שיקולי Platform (Windows/Mac/Linux)

הגדרות שעשויות להשתנות בין פלטפורמות:

```yaml
# Windows
output_folder: '{project-root}\docs'
shell: 'powershell'

# Mac/Linux
output_folder: '{project-root}/docs'
shell: 'bash'
```

**טיפ:** השתמשו ב-`{project-root}` תמיד - BMAD מתרגם אוטומטית לפורמט הנכון!

---

## ניהול גרסאות (Version Control)

### מה להכניס ל-Git ומה לא

#### ✅ חובה להכניס ל-Git

**קבצי צוות:**
```
bmad/_cfg/                           # כל התיקייה
├── agents/*.customize.yaml          # התאמות אג'נטים
├── agent-manifest.csv
├── workflow-manifest.csv
└── manifest.yaml

team-config-template.yaml            # Template לצוות
docs/                                # מסמכי פרויקט
README-bmad.md                       # הנחיות צוות
```

**למה?** אלה הגדרות ברמת הפרויקט שצריך לשתף בין הצוות.

#### ❌ לא להכניס ל-Git

**קבצי משתמש:**
```
bmad/core/config.yaml                # הגדרות אישיות
bmad/*/config.yaml                   # הגדרות מודולים אישיות
bmad-local.config.yaml               # Override אישי
.env                                 # משתני סביבה רגישים
```

**למה?** אלה ייחודיים לכל משתמש ומכילים מידע אישי.

#### ⚠️ אופציונלי (החליטו בצוות)

**קבצי IDE:**
```
.claude/                             # Claude Code commands
.cursor/                             # Cursor rules  
.windsurf/                           # Windsurf workflows
.vscode/settings.json                # VS Code settings
```

**שיקול:** אם הצוות משתמש באותו IDE - אפשר לשתף. אחרת - ignore.

### קובץ .gitignore מומלץ

```gitignore
# ====================
# BMAD Configuration
# ====================

# Personal settings (each team member has their own)
bmad/core/config.yaml
bmad/bmm/config.yaml
bmad/bmb/config.yaml
bmad/cis/config.yaml

# Local overrides
bmad-local.config.yaml
*.local.yaml

# Keep team-level customizations
!bmad/_cfg/

# ====================
# IDE Integration
# ====================

# Choose ONE strategy per team:
# Strategy A: Ignore all IDE files (each dev sets up their own)
.claude/
.cursor/
.windsurf/
.vscode/
.clinerules/
.roomodes/

# Strategy B: Share IDE files (uncomment if team agrees)
# !.claude/
# !.cursor/
# But still ignore user-specific IDE settings
.vscode/settings.json
.idea/workspace.xml

# ====================
# BMAD Outputs
# ====================

# Generated docs - usually INCLUDE these
# docs/

# But exclude temp/backup files
docs/**/*.tmp
docs/**/*.backup
docs/**/.DS_Store

# ====================
# BMAD Backups
# ====================

v4-backup/                           # v4 to v6 upgrade backup
*.bak
*~

# ====================
# Node.js
# ====================

node_modules/
npm-debug.log
yarn-error.log

# ====================
# OS Files
# ====================

.DS_Store
Thumbs.db
```

### Secrets Management

**לעולם אל תשימו ב-Git:**
- API keys
- Passwords
- Access tokens
- Personal paths

**במקום זאת:**

**אופציה 1: .env Files**
```bash
# .env (לא ב-Git!)
BMAD_API_KEY=your-secret-key
BMAD_OUTPUT_PATH=/secure/path
```

```yaml
# config.yaml
api_key: ${BMAD_API_KEY}
output_folder: ${BMAD_OUTPUT_PATH}
```

**אופציה 2: Git Secret/Vault**
```bash
# Install git-secret
brew install git-secret  # Mac
apt install git-secret   # Linux

# Encrypt sensitive files
git secret add bmad/core/config.yaml
git secret hide
```

**אופציה 3: Config Management Tool**
- HashiCorp Vault
- AWS Secrets Manager
- Azure Key Vault

### תהליך Pull Request עם קונפיג

**כאשר PR משנה config:**

```markdown
## Configuration Changes

**Files Changed:**
- `bmad/_cfg/agents/bmm-dev.customize.yaml`

**Impact:**
- DEV agent now includes TypeScript best practices
- All team members need to run: `npx bmad-method build bmm-dev`

**Testing:**
- [ ] Tested on dev environment
- [ ] Team members notified
- [ ] Documentation updated
```

**Checklist למחבר PR:**
- [ ] שינויי config מתועדים ב-PR description
- [ ] תיעדתי את ההשפעה על חברי צוות אחרים
- [ ] עדכנתי את team-config-template.yaml
- [ ] בדקתי שלא commitתי קבצים אישיים
- [ ] הרצתי `git status` לוודא מה הולך ל-Git

---

## גיבוי ושחזור קונפיגורציה

### למה צריך גיבוי?

- **Rollback מהיר:** אם משהו השתבש, חזרה למצב עובד
- **Recovery:** אם קבצים נמחקו בטעות
- **Migration:** מעבר למחשב חדש
- **Testing:** ניסוי של שינויים בלי פחד

### אסטרטגיית גיבוי מומלצת

#### Level 1: Git (גיבוי בסיסי)

אם _cfg/ ב-Git, יש לכם היסטוריה מלאה:

```bash
# חזרה לגרסה קודמת
git checkout HEAD~1 -- bmad/_cfg/

# חזרה לcommit ספציפי
git checkout abc1234 -- bmad/_cfg/

# שחזור קובץ בודד
git checkout main -- bmad/_cfg/agents/bmm-dev.customize.yaml
```

#### Level 2: Manual Backup (גיבוי ידני)

**לפני שינוי גדול:**

```bash
# יצירת גיבוי עם timestamp
DATE=$(date +%Y%m%d-%H%M%S)
tar -czf "bmad-backup-$DATE.tar.gz" bmad/
```

**שחזור:**

```bash
# רשימת גיבויים
ls -lh bmad-backup-*.tar.gz

# שחזור מגיבוי
tar -xzf bmad-backup-20241105-143022.tar.gz
```

#### Level 3: Automated Backup (גיבוי אוטומטי)

**סקריפט גיבוי אוטומטי:**

```bash
#!/bin/bash
# backup-bmad.sh

BACKUP_DIR="$HOME/.bmad-backups"
DATE=$(date +%Y%m%d-%H%M%S)
PROJECT_NAME=$(basename "$(pwd)")

mkdir -p "$BACKUP_DIR"

# גיבוי config + customizations
tar -czf "$BACKUP_DIR/$PROJECT_NAME-$DATE.tar.gz" \
  bmad/core/config.yaml \
  bmad/bmm/config.yaml \
  bmad/bmb/config.yaml \
  bmad/cis/config.yaml \
  bmad/_cfg/

# שמירת 10 גיבויים אחרונים בלבד
ls -t "$BACKUP_DIR/$PROJECT_NAME-"*.tar.gz | tail -n +11 | xargs rm -f

echo "✅ Backup created: $BACKUP_DIR/$PROJECT_NAME-$DATE.tar.gz"
```

**הוספה ל-Cron:**

```bash
# Edit crontab
crontab -e

# גיבוי יומי בחצות
0 0 * * * /path/to/backup-bmad.sh
```

#### Level 4: Cloud Backup (גיבוי ענן)

**Sync ל-cloud storage:**

```bash
# Dropbox
cp -r bmad/ ~/Dropbox/BMAD-Backups/myproject/

# OneDrive
cp -r bmad/ ~/OneDrive/BMAD-Backups/myproject/

# AWS S3
aws s3 sync bmad/ s3://my-bucket/bmad-backups/myproject/
```

### תהליך שחזור (Disaster Recovery)

**תרחיש: כל קבצי התצורה נמחקו!**

**שלב 1: אל פאניקה**
```bash
# בדיקה מה קיים
ls -la bmad/
ls -la bmad/_cfg/
```

**שלב 2: נסה Git**
```bash
# אם ב-Git
git status
git checkout HEAD -- bmad/

# אם לא עובד
git log --all -- bmad/
```

**שלב 3: שחזור מגיבוי**
```bash
# רשימת גיבויים
ls -lh ~/.bmad-backups/

# שחזור אחרון
tar -xzf ~/.bmad-backups/myproject-latest.tar.gz
```

**שלב 4: התקנה מחדש (אופציה אחרונה)**
```bash
# אם אין גיבוי - reinstall
npx bmad-method@alpha install
# ענו על השאלות מחדש
```

### Best Practices לגיבוי

| תדירות | מה לגבות | איפה |
|---------|----------|------|
| **לפני כל שינוי גדול** | Config + _cfg/ | מקומי |
| **יומי** | כל bmad/ | Cloud |
| **שבועי** | כל הפרויקט | External drive |
| **לפני upgrade** | כל bmad/ + docs/ | מקומי + Cloud |

---

## תרחישים נפוצים

### תרחיש 1: חבר צוות חדש מצטרף

**מצב התחלתי:**
- פרויקט קיים עם BMAD מוגדר
- חבר צוות חדש צריך להתחיל לעבוד

**פתרון (6 צעדים):**

**1. Clone הפרויקט:**
```bash
git clone <repository-url>
cd project-name
```

**2. התקנת BMAD:**
```bash
npx bmad-method@alpha install
```

**3. בחירת הגדרות אישיות:**
```
? What's your name? Sarah
? Communication language? Hebrew
? Document output language? English (מהtemplate של הצוות)
? IDE? Cursor
```

**4. אימות שההגדרות נכונות:**
```bash
cat bmad/core/config.yaml
# ודא ש-document_output_language: English (כמו הצוות)
```

**5. בנייה מחדש (אם יש customizations):**
```bash
npx bmad-method build
```

**6. בדיקה:**
```bash
# טען אג'נט
/bmad:core:agents:bmad-master
# או ב-Cursor: @bmad/core/agents/bmad-master
```

**תוצאה:** חבר הצוות מוכן לעבוד תוך 10 דקות!

---

### תרחיש 2: סנכרון שינויי Config בצוות

**מצב התחלתי:**
- Tech lead שינה התאמת אג'נט ב-`_cfg/`
- צריך לסנכרן לכל הצוות

**פתרון:**

**Tech Lead:**
```bash
# עריכת customization
nano bmad/_cfg/agents/bmm-dev.customize.yaml

# Commit ושיתוף
git add bmad/_cfg/agents/bmm-dev.customize.yaml
git commit -m "Update DEV agent: add TypeScript standards"
git push origin main
```

**שאר הצוות:**
```bash
# Pull השינויים
git pull origin main

# Rebuild האג'נט שהשתנה
npx bmad-method build bmm-dev

# או rebuild הכל
npx bmad-method build
```

**הודעה לצוות (Slack/Teams):**
```
📢 BMAD Config Update

Updated: DEV agent customization
Changes: Added TypeScript best practices
Action: Run `npx bmad-method build bmm-dev`
```

**תוצאה:** כל הצוות עם אותן הגדרות תוך 5 דקות.

---

### תרחיש 3: שינוי בין סביבות (Dev → Staging)

**מצב התחלתי:**
- עבדתם ב-dev
- צריך לעבור ל-staging לבדיקות

**פתרון:**

**אופציה 1: Branch Switching**
```bash
# Switch לbranch של staging
git checkout staging

# אם יש Git hook - config יתעדכן אוטומטית
# אחרת - עדכן ידנית:
cp configs/config-staging.yaml bmad/core/config.yaml
```

**אופציה 2: Environment Variable**
```bash
# הגדר סביבה
export BMAD_ENV=staging

# Restart IDE או terminal
# BMAD יטען את config-staging.yaml אוטומטית
```

**אופציה 3: Symlink**
```bash
# שנה את ה-symlink
ln -sf config-staging.yaml bmad/core/config.yaml

# אימות
cat bmad/core/config.yaml | grep output_folder
# צריך להראות: output_folder: '{project-root}/docs-staging'
```

**תוצאה:** עכשיו כל הפלטים הולכים ל-`docs-staging/` במקום `docs-dev/`.

---

### תרחיש 4: מיגרציה למחשב חדש

**מצב התחלתי:**
- עובדים על מחשב A
- קונים מחשב B חדש

**פתרון (4 שלבים):**

**במחשב הישן (A):**
```bash
# גיבוי הגדרות אישיות
cat bmad/core/config.yaml > ~/my-bmad-config.yaml

# Commit שינויים אחרונים
git add .
git commit -m "Final commit before migration"
git push
```

**במחשב החדש (B):**
```bash
# 1. Clone הפרויקט
git clone <repository-url>
cd project-name

# 2. התקנת BMAD
npx bmad-method@alpha install

# 3. שחזור הגדרות אישיות
# העתק את ~/my-bmad-config.yaml מהמחשב הישן
cp ~/my-bmad-config.yaml bmad/core/config.yaml

# 4. עדכן נתיבים אם צריך
nano bmad/core/config.yaml
# שנה נתיבים מוחלטים אם יש
```

**תוצאה:** עבודה רגילה על המחשב החדש תוך 15 דקות!

---

## פתרון בעיות תצורה

| בעיה | סיבה אפשרית | פתרון |
|------|-------------|--------|
| **Config לא נטען** | Syntax error ב-YAML | בדוק indentation, הרץ YAML validator |
| **האג'נט לא משתמש ב-customization** | לא הרצת build | `npx bmad-method build <agent-name>` |
| **שפת פלט שגויה** | `document_output_language` שונה בין חברי צוות | סנכרן עם team template |
| **קבצים נשמרים במקום הלא נכון** | `output_folder` מוחלט במקום יחסי | שנה ל-`{project-root}/docs` |
| **צוות לא מסונכרן** | _cfg/ לא ב-Git | `git add bmad/_cfg/` ו-push |
| **שינויים אבדו אחרי pull** | config.yaml ב-Git (לא צריך) | הוסף ל-.gitignore, שחזר מגיבוי |
| **IDE לא רואה אג'נטים** | קבצי IDE לא נוצרו | `npx bmad-method build` |
| **"Permission denied" על config** | הרשאות קובץ | `chmod 644 bmad/core/config.yaml` |
| **Config נעלם אחרי upgrade** | גיבוי לא נוצר | שחזר מ-`v4-backup/` או reinstall |
| **משתני סביבה לא עובדים** | סביבה לא נטענת | `source ~/.bashrc` או restart terminal |

---

## Best Practices

### 1. Configuration as Code

**עקרון:** קונפיגורציה היא חלק מהקוד - נהלו אותה כמו קוד.

**מה זה אומר:**
- ✅ Version control (Git) לקונפיג צוות
- ✅ Code reviews לשינויי config
- ✅ תיעוד של שינויים
- ✅ Testing של שינויים לפני merge

**דוגמה:**
```bash
# שינוי config עובר דרך PR
git checkout -b config/update-dev-agent
# עריכה...
git commit -m "Config: Add Python standards to DEV agent"
git push
# PR → Review → Merge
```

### 2. Principle of Least Surprise

**עקרון:** הגדרות צריכות להיות צפויות ועקביות.

**מה זה אומר:**
- כל הצוות משתמש באותן קונבנציות
- שינויים מתועדים ומוכרזים
- ברירות מחדל הגיוניות

**דוגמה טובה:**
```yaml
# כולם יודעים שזה הסטנדרט
document_output_language: English
output_folder: '{project-root}/docs'
```

**דוגמה רעה:**
```yaml
# כל אחד עושה מה שבא לו
output_folder: '/Users/alice/Desktop/my-docs'
document_output_language: Spanish  # למה ספרדית??
```

### 3. הפרדה ברורה: Personal vs Team

**כללי זהב:**

| הגדרה | Personal | Team | איפה |
|--------|----------|------|------|
| `user_name` | ✅ | ❌ | Local config |
| `communication_language` | ✅ | ❌ | Local config |
| `document_output_language` | ❌ | ✅ | Git |
| `output_folder` (relative) | ❌ | ✅ | Git |
| `*.customize.yaml` | ❌ | ✅ | Git |

### 4. תיעוד שינויים

**תמיד תעדו:**
- מה השתנה
- למה השתנה
- מי צריך לפעול

**Template להודעת שינוי:**
```markdown
## BMAD Config Change

**Date:** 2024-11-05
**Changed by:** @tech-lead
**Files:** bmad/_cfg/agents/bmm-architect.customize.yaml

**What:** Added security review checklist to Architect agent
**Why:** New company security requirements
**Action Required:**
- Run: `npx bmad-method build bmm-architect`
- Read: Updated security guidelines in docs/

**Questions?** Ask in #bmad-help
```

### 5. גיבוי לפני שינויים

**לפני כל שינוי משמעותי:**

```bash
# Quick backup
tar -czf bmad-backup-$(date +%Y%m%d).tar.gz bmad/

# עכשיו שנה בביטחון
```

### 6. בדיקה אחרי שינויים

**Checklist:**
- [ ] Config files תקינים (YAML syntax)
- [ ] Build עבר בהצלחה
- [ ] אג'נט טוען נכון
- [ ] Workflow רץ נכון
- [ ] צוות מודע לשינוי

### 7. Single Source of Truth

**עקרון:** יש מקור אמת אחד להגדרות צוות.

**מה זה אומר:**
- Git repository הוא ה-source of truth
- לא אלו מיילים, לא Google Docs
- כל השינויים דרך Git

**דוגמה:**
```
❌ "Hey, change your config to use English"
✅ PR #123: Update team config template
```

---

## שאלות נפוצות

**ש: מה ההבדל בין config.yaml ל-customize.yaml?**  
ת: `config.yaml` הוא הגדרות בסיסיות (שפה, נתיבים). `customize.yaml` הוא התאמות של אג'נטים ספציפיים (התנהגות, תפריטים, אישיות). השתמש ב-customize.yaml להתאמות צוות כי הוא לא נדרס בעדכונים.

**ש: האם כל הצוות חייב להשתמש באותה `communication_language`?**  
ת: לא! כל אחד יכול לבחור שפה שנוחה לו. רק `document_output_language` צריך להיות אחיד כדי שכל המסמכים יהיו באותה שפה.

**ש: מה קורה אם שניים עורכים אותו customize.yaml?**  
ת: Git conflict. פתרו כמו כל conflict אחר - merge ידנית או בחרו גרסה. זה למה חשוב לתאם שינויים גדולים בצוות.

**ש: איך אני יודע אם השינויים שלי הושמרו?**  
ת: הרץ `git status` ובדוק מה השתנה. אם `config.yaml` השתנה אבל הוא ב-.gitignore - זה בסדר (אישי). אם `*.customize.yaml` השתנה - commit אותו.

**ש: האם אני צריך להריץ build אחרי כל pull?**  
ת: רק אם קבצי `_cfg/` השתנו. בדוק את הפלט של `git pull` - אם רואים `bmad/_cfg/...` - הרץ `npx bmad-method build`.

**ש: מה אם אני רוצה output folder שונה רק לפרויקט אחד?**  
ת: עדכן את `bmad/core/config.yaml` באותו פרויקט. רק וודא שאם זה פרויקט צוות - תשתמש בנתיב יחסי, לא מוחלט.

**ש: האם אפשר לשתף config בין כמה פרויקטים?**  
ת: כן, עם symlink. אבל לא מומלץ - כל פרויקט צריך config משלו. אפשר לשתף templates.

**ש: איך מגבים קונפיג אוטומטית?**  
ת: הוסף סקריפט גיבוי ל-cron (Linux/Mac) או Task Scheduler (Windows). ראה בסעיף "גיבוי ושחזור".

**ש: מה עושים אם config.yaml נמחק בטעות?**  
ת: אם ב-Git - `git checkout HEAD -- bmad/core/config.yaml`. אם לא - שחזר מגיבוי או הרץ `npx bmad-method@alpha install` מחדש.

**ש: האם BMAD תומך במשתני סביבה בconfig?**  
ת: לא ישירות ב-v6, אבל אפשר להשתמש בסקריפט wrapper שממיר משתנים לconfig לפני הרצה.

**ש: איך משנים config באמצע עבודה?**  
ת: עדכן את `config.yaml` ו-restart את האג'נט. אין צורך ל-restart את ה-IDE.

**ש: מה זה {project-root}?**  
ת: Placeholder שBMAD מתרגם אוטומטית לנתיב הפרויקט. השתמש בו תמיד במקום נתיב מוחלט.

---

## משאבים נוספים

### מדריכים קשורים

- [התקנה והגדרה מתקדמת](../04-התקנה-והגדרה-מתקדמת.md) - התקנה מפורטת של BMAD
- [עדכונים ושדרוגים](./עדכונים-ושדרוגים.md) - איך לעדכן BMAD
- [שיטות עבודה מומלצות](../05-שיטות-עבודה-מומלצות.md) - Best practices (בקרוב)
- [Document Sharding Guide](../../docs/document-sharding-guide.md) - ניהול מסמכים גדולים

### כלים מומלצים

**YAML Validators:**
- [YAML Lint](http://www.yamllint.com/) - בדיקת syntax אונליין
- [VS Code YAML Extension](https://marketplace.visualstudio.com/items?itemName=redhat.vscode-yaml) - תמיכה ב-YAML

**Git Tools:**
- [Git Secret](https://git-secret.io/) - הצפנת קבצים רגישים ב-Git
- [GitGuardian](https://www.gitguardian.com/) - זיהוי secrets ב-commits

**Backup Tools:**
- [rsync](https://rsync.samba.org/) - סנכרון קבצים יעיל
- [rclone](https://rclone.org/) - גיבוי לcloud

### תמיכה

**קהילה:**
- Discord: https://discord.gg/gk8jAdXWmj
- GitHub Discussions: https://github.com/bmad-code-org/BMAD-METHOD/discussions

**דיווח בעיות:**
- GitHub Issues: https://github.com/bmad-code-org/BMAD-METHOD/issues

**תיעוד מפורט:**
- [BMAD Documentation](../../README.md)
- [BMM Module Docs](../../src/modules/bmm/README.md)

---

## צ'קליסט ניהול תצורה

### הגדרה ראשונית

- [ ] קובץ `.gitignore` מוגדר נכון
- [ ] `team-config-template.yaml` נוצר ו-committed
- [ ] כל חברי הצוות מודעים לקונבנציות
- [ ] מדריך "BMAD Configuration Guidelines" נכתב
- [ ] הוגדר מי אחראי על שינויי config

### תחזוקה שוטפת

- [ ] גיבוי אוטומטי פועל (יומי/שבועי)
- [ ] בדיקה חודשית שכל הצוות מסונכרן
- [ ] עדכון `team-config-template.yaml` כשיש שינויים
- [ ] תיעוד כל שינוי config משמעותי
- [ ] Code review לכל שינוי ב-`_cfg/`

### לפני שינוי גדול

- [ ] גיבוי ידני של bmad/ כולו
- [ ] תיאום עם הצוות
- [ ] תכנון rollback אם משהו ישתבש
- [ ] בדיקה בסביבת dev לפני production

### אחרי שינוי

- [ ] `npx bmad-method build` רץ בהצלחה
- [ ] בדיקה שהאג'נטים עובדים
- [ ] הודעה לצוות על השינוי
- [ ] עדכון תיעוד אם נדרש
- [ ] Commit ו-push (אם זה team setting)

### טיפול בבעיות

- [ ] יש גישה לגיבויים אחרונים
- [ ] יודעים איך לשחזר מגיבוי
- [ ] יש איש קשר לבעיות config
- [ ] תיעדנו פתרונות לבעיות נפוצות

---

<div dir="rtl" align="center">

**🎉 מזל טוב! השלמתם את המדריך לניהול תצורה!**

עכשיו אתם מומחים בניהול קונפיגורציה של BMAD בצוותים.  
זמן ליישם את הידע! 🚀

---

*נוצר עבור BMAD-METHOD v6 | עדכון אחרון: נובמבר 2024*

[🏠 חזרה לדף הבית](../README.md) | [📚 כל המדריכים](../README.md#-מדריכים-זמינים) | [⬆️ עדכונים ושדרוגים](./עדכונים-ושדרוגים.md)

</div>