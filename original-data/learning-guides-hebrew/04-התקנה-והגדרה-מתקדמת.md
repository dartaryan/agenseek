# התקנה והגדרה מתקדמת ל-BMAD-METHOD

> **מדריך מקיף** להתקנה מתקדמת, אינטגרציה ל-IDE, וקונפיגורציה של BMAD

---

## 📋 תוכן עניינים

### [חלק 1: לפני ההתקנה](#חלק-1-לפני-ההתקנה)
- [דרישות מערכת מפורטות](#דרישות-מערכת-מפורטות)
- [התקנת Node.js בכל המערכות](#התקנת-nodejs-בכל-המערכות)
- [הכנת סביבת IDE](#הכנת-סביבת-ide)
- [שיקולי רשת ו-Firewall](#שיקולי-רשת-ו-firewall)

### [חלק 2: תרחישי התקנה](#חלק-2-תרחישי-התקנה)
- [התקנה נקייה (Clean Installation)](#התקנה-נקייה-clean-installation)
- [שדרוג מ-v4 ל-v6](#שדרוג-מ-v4-ל-v6)
- [התקנה סלקטיבית של מודולים](#התקנה-סלקטיבית-של-מודולים)
- [התקנה בסביבה ארגונית](#התקנה-בסביבה-ארגונית)
- [התקנה Offline](#התקנה-offline)
- [פתרון בעיות התקנה](#פתרון-בעיות-התקנה)

### [חלק 3: אינטגרציה ל-IDE](#חלק-3-אינטגרציה-ל-ide)
- [Claude Code - הגדרה מפורטת](#claude-code---הגדרה-מפורטת)
- [Cursor - הגדרה מפורטת](#cursor---הגדרה-מפורטת)
- [Windsurf - הגדרה מפורטת](#windsurf---הגדרה-מפורטת)
- [VS Code + GitHub Copilot](#vs-code--github-copilot)
- [Cline, Roo, ו-IDEs נוספים](#cline-roo-ו-ides-נוספים)
- [אינטגרציה מותאמת אישית](#אינטגרציה-מותאמת-אישית)
- [שיטות טעינת אג'נטים](#שיטות-טעינת-אגנטים)
- [קונפיגורציית Slash Commands](#קונפיגורציית-slash-commands)

### [חלק 4: קונפיגורציה מתקדמת](#חלק-4-קונפיגורציה-מתקדמת)
- [מבנה config.yaml](#מבנה-configyaml)
- [הגדרות משתמש](#הגדרות-משתמש)
- [הגדרות שפה (תקשורת מול פלט)](#הגדרות-שפה-תקשורת-מול-פלט)
- [קונפיגורציית תיקיות פלט](#קונפיגורציית-תיקיות-פלט)
- [קונפיגורציה ספציפית למודול](#קונפיגורציה-ספציפית-למודול)
- [קבצי התאמה אישית של אג'נטים](#קבצי-התאמה-אישית-של-אגנטים)
- [יצירת אג'נט מותאם אישית - יסודות](#יצירת-אגנט-מותאם-אישית---יסודות)

### [חלק 5: נושאים מתקדמים](#חלק-5-נושאים-מתקדמים)
- [הגדרת פרויקטים מרובים](#הגדרת-פרויקטים-מרובים)
- [סנכרון קונפיגורציה של צוות](#סנכרון-קונפיגורציה-של-צוות)
- [שיטות עבודה מומלצות ב-Version Control](#שיטות-עבודה-מומלצות-ב-version-control)
- [המלצות .gitignore](#המלצות-gitignore)
- [אינטגרציה עם CI/CD](#אינטגרציה-עם-cicd)

---

## חלק 1: לפני ההתקנה

### דרישות מערכת מפורטות

#### דרישות חומרה מינימליות

**למערכות פיתוח רגילות:**
- **מעבד:** Intel i5 / AMD Ryzen 5 דור 6 ומעלה (או שווה ערך)
- **זיכרון RAM:** 8GB מינימום, 16GB מומלץ
- **שטח דיסק:** 500MB להתקנת BMAD בסיסית
- **שטח דיסק נוסף:** 2-5GB למסמכי פרויקט (תלוי בגודל הפרויקט)
- **חיבור אינטרנט:** נדרש להתקנה ראשונית ועדכונים

**למערכות Enterprise או פרויקטים גדולים:**
- **מעבד:** Intel i7 / AMD Ryzen 7 דור 8 ומעלה
- **זיכרון RAM:** 32GB מומלץ (לעבודה עם מודלי AI גדולים)
- **שטח דיסק:** 10-20GB לפרויקטים מורכבים
- **SSD מומלץ:** לביצועים מיטביים

#### דרישות תוכנה

**חובה:**
- **Node.js:** גרסה 20.0.0 ומעלה (LTS מומלץ)
  - גרסה נוכחית (נובמבר 2024): v20.x.x או v22.x.x
  - לא תומך בגרסאות ישנות (v16, v18)
- **npm:** גרסה 9.0.0 ומעלה (מגיע אוטומטית עם Node.js)

**מומלץ:**
- **IDE/Code Editor:** אחד מהבאים:
  - Claude Code (מומלץ מאוד)
  - Cursor
  - Windsurf
  - VS Code עם GitHub Copilot
  - Cline, Roo, Auggie, או IDEs נוספים
- **Git:** לניהול גרסאות (אופציונלי אך מומלץ מאוד)

#### תאימות מערכות הפעלה

**Windows:**
- Windows 10 (1809 ומעלה)
- Windows 11 (כל הגרסאות)
- Windows Server 2019/2022

**macOS:**
- macOS 11 Big Sur ומעלה
- תמיכה בשבבי Intel ו-Apple Silicon (M1/M2/M3)

**Linux:**
- Ubuntu 20.04 LTS ומעלה
- Debian 11 ומעלה
- Fedora 35 ומעלה
- CentOS 8 / RHEL 8 ומעלה
- כל דיסטרו מבוסס Debian/Red Hat עם glibc 2.28+

---

### התקנת Node.js בכל המערכות

#### Windows - שיטות התקנה

**שיטה 1: התקנה רגילה (מומלץ למתחילים)**

1. **הורידו את המתקין:**
   - גשו ל-https://nodejs.org
   - בחרו בגרסת **LTS** (Long Term Support)
   - הורידו את `Windows Installer (.msi)` - 64-bit

2. **הריצו את המתקין:**
   - הקליקו פעמיים על הקובץ שהורדתם
   - בחרו "Next" בכל שלב
   - **חשוב:** ודאו שהאופציה "Add to PATH" מסומנת ✅
   - האופציה "Automatically install necessary tools" - אופציונלית

3. **אשרו את ההתקנה:**
   ```powershell
   node --version
   npm --version
   ```

**שיטה 2: התקנה עם Chocolatey (למתקדמים)**

```powershell
# התקנת Chocolatey (אם אין)
Set-ExecutionPolicy Bypass -Scope Process -Force
[System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))

# התקנת Node.js
choco install nodejs-lts -y

# רענון המסוף
refreshenv
```

**שיטה 3: התקנה עם nvm-windows (לניהול גרסאות מרובות)**

```powershell
# הורידו nvm-windows מ-https://github.com/coreybutler/nvm-windows/releases
# הריצו את המתקין, ואז:

nvm install 20
nvm use 20
```

#### macOS - שיטות התקנה

**שיטה 1: התקנה עם Homebrew (מומלץ)**

```bash
# התקנת Homebrew (אם אין)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# התקנת Node.js
brew install node@20

# הוספה ל-PATH (אם נדרש)
echo 'export PATH="/usr/local/opt/node@20/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

**שיטה 2: התקנה רגילה מהאתר**

1. גשו ל-https://nodejs.org
2. הורידו את מתקין macOS (.pkg) - גרסת LTS
3. הריצו את המתקין ועקבו אחרי ההוראות

**שיטה 3: התקנה עם nvm (לניהול גרסאות מרובות)**

```bash
# התקנת nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# רענון הפרופיל
source ~/.zshrc  # או ~/.bash_profile

# התקנת Node.js
nvm install 20
nvm use 20
nvm alias default 20
```

#### Linux - שיטות התקנה

**Ubuntu/Debian:**

```bash
# עדכון מאגרי החבילות
sudo apt update

# התקנת Node.js 20.x
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# אימות התקנה
node --version
npm --version
```

**Fedora/RHEL/CentOS:**

```bash
# התקנת Node.js 20.x
sudo dnf module install nodejs:20

# אימות התקנה
node --version
npm --version
```

**שיטה אוניברסלית - nvm (מומלץ למפתחים)**

```bash
# התקנת nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# הוספה ל-shell profile
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

# התקנת Node.js
nvm install 20
nvm use 20
nvm alias default 20
```

---

### הכנת סביבת IDE

#### בחירת IDE מתאים

לפני ההתקנה, חשוב לבחור IDE שמתאים לצרכים שלכם:

**Claude Code** ⭐ מומלץ מאוד
- אינטגרציה מובנית עם Claude AI
- תמיכה ב-slash commands מהירים
- חוויית משתמש מצוינת לעבודה עם אג'נטים
- **מתי לבחור:** אם אתם עובדים בעיקר עם Claude AI

**Cursor** ⭐ מומלץ מאוד
- תמיכה ב-MDC rules (Manual Documentation Context)
- גמישות בניהול context
- אינטגרציה טובה עם AI models שונים
- **מתי לבחור:** אם אתם צריכים שליטה מדויקת ב-context

**Windsurf** ⭐ מומלץ
- תמיכה ב-workflows כישויות מובנות
- אוטונומיה גבוהה לאג'נטים
- **מתי לבחור:** לפרויקטים שדורשים אוטומציה גבוהה

**VS Code + GitHub Copilot**
- הכי פופולרי, קהילה ענקית
- תמיכה ב-chat modes
- **מתי לבחור:** אם כבר עובדים עם VS Code

**IDEs נוספים:** Cline, Roo, Auggie - כולם נתמכים!

#### הכנת IDE לקראת התקנה

**כל ה-IDEs:**
1. וודאו שה-IDE מעודכן לגרסה האחרונה
2. התקינו את הרחבות AI הרלוונטיות (אם נדרש)
3. התחברו לשירות AI שלכם (Claude, GPT, וכו')
4. צרו או פתחו את תיקיית הפרויקט ב-IDE

**Claude Code:**
```bash
# ודאו שתיקיית .claude קיימת (תיווצר אוטומטית)
# אם לא - צרו אותה ידנית:
mkdir .claude
mkdir .claude/commands
```

**Cursor:**
```bash
# ודאו שתיקיית .cursor קיימת
mkdir .cursor
mkdir .cursor/rules
```

**Windsurf:**
```bash
# ודאו שתיקיית .windsurf קיימת
mkdir .windsurf
mkdir .windsurf/workflows
```

---

### שיקולי רשת ו-Firewall

#### חיבורי אינטרנט נדרשים

BMAD צריך גישה לשרתים הבאים:

**בזמן התקנה:**
- `registry.npmjs.org` - להורדת חבילת BMAD
- `nodejs.org` - לאימות גרסאות
- GitHub (אופציונלי) - למודולים נוספים

**בזמן שימוש:**
- שירותי AI שאתם משתמשים בהם (Claude API, OpenAI API, וכו')
- אין צורך בחיבור רציף לשרתי BMAD

#### הגדרות Firewall וסביבות ארגוניות

**אם אתם מאחורי Proxy ארגוני:**

```bash
# הגדרת proxy ל-npm
npm config set proxy http://proxy.company.com:8080
npm config set https-proxy http://proxy.company.com:8080

# אם יש צורך באימות
npm config set proxy http://user:password@proxy.company.com:8080
```

**אם אתם בסביבה עם SSL certificates מותאמים:**

```bash
# כיבוי בדיקת SSL (לא מומלץ, אבל לפעמים נדרש)
npm config set strict-ssl false

# או - הוספת CA certificate
npm config set cafile /path/to/company-ca.crt
```

**Whitelist URLs לצוות IT:**

אם הצוות IT צריך לאשר גישה, בקשו לפתוח:
- `*.npmjs.org` (port 443)
- `*.nodejs.org` (port 443)
- שירותי AI הרלוונטיים (תלוי במה אתם משתמשים)

---

## חלק 2: תרחישי התקנה

### התקנה נקייה (Clean Installation)

#### תרחיש: פרויקט חדש מאפס

זהו התרחיש הפשוט ביותר - אתם מתחילים פרויקט חדש עם BMAD.

**שלב 1: יצירת תיקיית פרויקט**

```bash
# Windows
cd C:\Projects
mkdir MyNewProject
cd MyNewProject

# macOS/Linux
cd ~/Projects
mkdir MyNewProject
cd MyNewProject
```

**שלב 2: הרצת ההתקנה**

```bash
npx bmad-method@alpha install
```

**שלב 3: עקבו אחרי הקסם**

המתקין ישאל אתכם סדרה של שאלות:

**שאלה 1: בחירת מודולים**
```
? Which modules would you like to install?
❯ ◉ BMM (BMad Method) - Software & Game Development
  ◉ BMB (BMad Builder) - Create Custom Agents
  ◉ CIS (Creative Intelligence) - Innovation & Creativity
```

**טיפ:** לפרויקט פיתוח תוכנה רגיל - בחרו BMM. למי שרוצה הכל - בחרו את כולם!

**שאלה 2: שם המשתמש**
```
? What's your name? (for agent personalization)
```

הכניסו את שמכם - האג'נטים יפנו אליכם בשם!

**שאלה 3: שפת תקשורת**
```
? Communication language? (How agents talk to you)
❯ English
  Hebrew
  Spanish
  [Other languages...]
```

**שאלה 4: שפת פלט למסמכים**
```
? Document output language? (PRD, Architecture, etc.)
❯ English
  Hebrew
  [Same as communication]
```

**טיפ:** אפשר לבחור עברית לתקשורת ואנגלית למסמכים!

**שאלה 5: בחירת IDE**
```
? Which IDE(s) are you using?
❯ ◉ Claude Code
  ◯ Cursor
  ◯ Windsurf
  ◯ VS Code (GitHub Copilot)
  ◯ Other
```

בחרו אחד או יותר (רווח = סימון, Enter = המשך)

**שאלות נוספות (תלוי ב-IDE שבחרתם):**

כל IDE עשוי לשאול שאלות ספציפיות:
- Claude Code: האם להפעיל project context
- GitHub Copilot: מספר בקשות מקסימלי
- ועוד...

**שלב 4: המתינו להשלמת ההתקנה**

המתקין יבצע:
1. ✅ הורדת מודולים (10-30 שניות)
2. ✅ יצירת מבנה תיקיות
3. ✅ יצירת קבצי קונפיגורציה
4. ✅ הגדרת אינטגרציית IDE
5. ✅ יצירת manifests

**שלב 5: אימות הצלחה**

כשמסתיים, תראו:
```
✅ BMAD installed successfully!

📁 Files created:
   - bmad/ (core, bmm, bmb, cis, _cfg)
   - .claude/commands/ (if Claude Code selected)
   - .cursor/rules/ (if Cursor selected)

🎯 Next steps:
   1. Load bmad/core/agents/bmad-master.md in your IDE
   2. Run *workflow-init to set up your workflow
   3. Start building! 🚀
```

**בדיקה ידנית:**

```bash
# בדקו שהתיקייה נוצרה
ls bmad/
# אמור להראות: core, bmm, _cfg (ומודולים נוספים אם בחרתם)

# בדקו שהקונפיג נוצר
cat bmad/core/config.yaml
```

---

### שדרוג מ-v4 ל-v6

#### תרחיש: יש לכם BMAD v4 מותקן

זהו תרחיש נפוץ למשתמשים ותיקים. v6 זיהה את המערכת הישנה ויטפל בה אוטומטית.

**הבנת השינויים המבניים**

**v4 היה:**
```
your-project/
├── .bmad-core/        # למעשה היה BMad Method
├── .bmad-game-dev/    # מודול נפרד
└── .bmad-infrastructure-devops/
```

**v6 הוא:**
```
your-project/
└── bmad/
    ├── core/          # Core אמיתי (חדש!)
    ├── bmm/           # BMad Method (כולל game dev)
    ├── bmb/           # BMad Builder
    ├── cis/           # Creative Intelligence
    └── _cfg/          # ההתאמות שלכם
```

**שלב 1: גיבוי (מומלץ מאוד)**

```bash
# גבו את כל הפרויקט
cp -r /path/to/project /path/to/project-backup

# או לפחות את תיקיות v4
mkdir v4-manual-backup
cp -r .bmad-* v4-manual-backup/
```

**שלב 2: הריצו את המתקין**

```bash
npx bmad-method@alpha install
```

**שלב 3: זיהוי אוטומטי של v4**

המתקין יזהה:
```
🔍 Detected v4 installation!

Found:
  - .bmad-core (v4.2.0)
  - .bmad-2d-unity-game-dev

📦 v4 Backup:
  All .bmad-* folders will be moved to: v4-backup/

⚠️ IDE Command Cleanup:
  Old v4 commands found in .claude/commands/
  Recommended: Remove manually after installation
```

**שלב 4: אישור הגיבוי**

```
? Move v4 folders to v4-backup/?
❯ Yes (recommended)
  No (I'll handle it manually)
```

בחרו **Yes** - זה בטוח לחלוטין!

**שלב 5: המשך התקנה רגילה**

מכאן זה כמו התקנה נקייה - עקבו אחרי השאלות.

**שלב 6: ניקוי ידני של IDE Commands (מומלץ)**

המתקין יציג לכם פקודות להעתקה:

**Windows:**
```powershell
# העתיקו והריצו:
Remove-Item -Recurse -Force .claude\commands\bmad
Remove-Item -Recurse -Force .cursor\commands\bmad
```

**macOS/Linux:**
```bash
# העתיקו והריצו:
rm -rf .claude/commands/bmad
rm -rf .cursor/commands/bmad
```

**שלב 7: מיגרציית התקדמות הפרויקט**

אם השלמתם phase 2 (PRD/Architecture) ב-v4:

1. **הריצו workflow-init:**
   ```
   /bmad:core:workflows:workflow-init
   ```

2. **ציינו את רמת הפרויקט:**
   - בחרו **Level 3 או 4** (כבר עברתם planning)
   - המערכת תדלג על PRD/Architecture

3. **שמרו על נתיבי המסמכים:**
   - אם ה-PRD שלכם ב-`docs/` - השאירו כך
   - v6 תומך גם במסמכים שלמים וגם ב-sharded

---

### התקנה סלקטיבית של מודולים

#### תרחיש: אתם צריכים רק מודול אחד

לא כולם צריכים את כל המודולים. BMAD מאפשר בחירה סלקטיבית.

**שלב 1: הבינו מה כל מודול עושה**

- **core:** נדרש תמיד (מכיל BMad Master + framework)
- **bmm (BMad Method):** פיתוח תוכנה ומשחקים
- **bmb (BMad Builder):** יצירת אג'נטים ווורקפלואים מותאמים
- **cis (Creative Intelligence):** סיעור מוחות, חדשנות, יצירתיות

**שלב 2: התקנה**

```bash
npx bmad-method@alpha install
```

**שלב 3: בחרו רק מה שאתם צריכים**

```
? Which modules would you like to install?
❯ ◉ BMM (BMad Method)
  ◯ BMB (BMad Builder)
  ◯ CIS (Creative Intelligence)
```

השתמשו ב-**Space** כדי לבטל סימון של מודולים שאתם לא צריכים.

**דוגמאות לתרחישים:**

**מפתח שרק רוצה את BMM:**
```
✓ BMM only
✗ BMB
✗ CIS
```

**מישהו שרוצה ליצור אג'נטים מותאמים:**
```
✗ BMM
✓ BMB only
✗ CIS
```

**מנהל חדשנות:**
```
✗ BMM
✗ BMB
✓ CIS only
```

**שלב 4: הוספת מודולים מאוחר יותר**

רוצים להוסיף מודול? הריצו את המתקין שוב:

```bash
npx bmad-method@alpha install
```

המתקין יזהה:
```
🔍 Detected existing installation!

Currently installed:
  ✓ core
  ✓ bmm

? Install additional modules?
❯ ◯ BMB (BMad Builder)
  ◯ CIS (Creative Intelligence)
```

סמנו מה שחסר ו-BMAD יוסיף רק את זה!

---

### התקנה בסביבה ארגונית

#### תרחיש: התקנה במספר מחשבים בארגון

**אתגרים נפוצים בארגונים:**
- מדיניות אבטחה מחמירה
- Proxy servers
- אישורים של IT
- רצון לאחידות בין משתמשים

**אסטרטגיה 1: התקנה סטנדרטית עם קונפיגורציה משותפת**

**שלב 1: צרו "Golden Configuration"**

מחשב אחד (admin/lead) מתקין ומגדיר:

```bash
npx bmad-method@alpha install
```

השלימו את כל השאלות עם הגדרות ארגוניות:
- שפת תקשורת: English (או לפי מדיניות)
- שפת פלט: English
- IDE: לפי מה שהארגון משתמש בו

**שלב 2: שמרו את קובץ הקונפיג הארגוני**

```bash
# העתיקו את הקונפיג למקום מרכזי
cp bmad/core/config.yaml ~/company-bmad-config.yaml
```

**שלב 3: חלקו את הקונפיג לצוות**

שלחו את `company-bmad-config.yaml` דרך:
- Git repository משותף
- תיקייה משותפת ברשת
- מערכת ניהול קונפיגורציה

**שלב 4: התקנה על מחשבי הצוות**

כל חבר צוות:
```bash
# התקנה רגילה
npx bmad-method@alpha install

# החלפת הקונפיג בארגוני
cp ~/company-bmad-config.yaml bmad/core/config.yaml
```

**אסטרטגיה 2: סקריפט התקנה אוטומטי**

יצרו סקריפט שמתקין אוטומטית:

```bash
#!/bin/bash
# install-bmad-company.sh

# התקנה עם ברירות מחדל ארגוניות
npx bmad-method@alpha install --auto \
  --modules=bmm,bmb \
  --user-name="Developer" \
  --lang=English \
  --output-lang=English \
  --ide=cursor

echo "✅ BMAD installed with company defaults"
```

**שלב 5: התמודדות עם Proxy ו-Firewall**

הוסיפו להנחיות:
```bash
# הגדרות proxy (לפני ההתקנה)
npm config set proxy http://proxy.company.com:8080
npm config set https-proxy http://proxy.company.com:8080

# ואז התקנה רגילה
npx bmad-method@alpha install
```

---

### התקנה Offline

#### תרחיש: אין חיבור לאינטרנט במחשב היעד

זה מסובך יותר אבל אפשרי.

**שלב 1: הורידו את החבילה במחשב עם אינטרנט**

```bash
# צרו תיקייה להורדה
mkdir bmad-offline-package
cd bmad-offline-package

# הורידו את bmad-method ואת כל התלויות
npm pack bmad-method@alpha
npm install bmad-method@alpha --prefix ./temp
cp -r ./temp/node_modules ./

# ארזו הכל
cd ..
tar -czf bmad-offline.tar.gz bmad-offline-package/
```

**שלב 2: העבירו למחשב ללא אינטרנט**

העבירו את `bmad-offline.tar.gz` דרך:
- USB drive
- העברת קבצים פנימית
- CD/DVD (למקרים קיצוניים)

**שלב 3: התקנה במחשב היעד**

```bash
# חלצו את הארכיון
tar -xzf bmad-offline.tar.gz
cd bmad-offline-package

# התקינו גלובלית (אופציונלי)
npm install -g ./bmad-method-*.tgz

# או הריצו ישירות
npx ./bmad-method-*.tgz install
```

**מגבלות:**
- המתקין לא יוכל לעדכן אוטומטית
- תצטרכו להוריד עדכונים ידנית
- חלק מהפיצ'רים שדורשים אינטרנט לא יעבדו

---

### פתרון בעיות התקנה

#### 15+ בעיות נפוצות ופתרונות

**בעיה 1: `EACCES` - Permission Denied**

**תסמינים:**
```
npm ERR! code EACCES
npm ERR! syscall mkdir
npm ERR! path /usr/local/lib/node_modules
```

**פתרון:**
```bash
# Linux/Mac - תקנו הרשאות npm
sudo chown -R $(whoami) ~/.npm
sudo chown -R $(whoami) /usr/local/lib/node_modules

# Windows - הריצו PowerShell כ-Administrator
```

**בעיה 2: `MODULE_NOT_FOUND` - חסרה תלות**

**פתרון:**
```bash
# נקו cache והתקינו מחדש
npm cache clean --force
rm -rf node_modules package-lock.json
npx bmad-method@alpha install
```

**בעיה 3: `ETIMEDOUT` - Network timeout**

**פתרון:**
```bash
# הגדילו timeout
npm config set timeout 60000

# נסו registry חלופי
npm config set registry https://registry.npmjs.org/

# או השתמשו ב-yarn
npm install -g yarn
yarn global add bmad-method@alpha
```

**בעיה 4: התקנה תקועה ב-"Installing..." **

**פתרון:**
```bash
# בטלו (Ctrl+C) ונסו עם verbose
npx bmad-method@alpha install --verbose

# זה יראה לכם איפה זה תקוע
```

**בעיה 5: שגיאת `gyp` ב-Windows**

**פתרון:**
```bash
# התקינו build tools
npm install --global windows-build-tools

# או התקינו Visual Studio Build Tools ידנית
```

**בעיה 6: "Command not found" אחרי התקנה**

**פתרון:**
```bash
# סגרו ופתחו מחדש את Terminal
# או רעננו את PATH:

# Windows PowerShell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")

# Mac/Linux
source ~/.bashrc  # או ~/.zshrc
```

**בעיה 7: IDE לא מזהה את האג'נטים**

**פתרון:**
```bash
# בדקו שהתיקיות נוצרו:
ls .claude/commands/   # לClaude Code
ls .cursor/rules/      # לCursor
ls .windsurf/workflows/  # לWindsurf

# אם חסרות - הריצו rebuild:
npx bmad-method build
```

**בעיה 8: שגיאת "Invalid configuration"**

**פתרון:**
```bash
# בדקו syntax של config.yaml
cat bmad/core/config.yaml

# תקנו YAML errors (בדרך כלל indentation)
# או מחקו ותנו למתקין ליצור מחדש:
rm bmad/core/config.yaml
npx bmad-method@alpha install
```

**בעיה 9: "Version mismatch" בעדכון**

**פתרון:**
```bash
# אלצו התקנה נקייה
npm cache clean --force
npx bmad-method@alpha install --force
```

**בעיה 10: התקנה איטית מאוד**

**פתרון:**
```bash
# השתמשו ב-fast registry
npm config set registry https://registry.npmmirror.com/

# או התקינו במקביל
npm config set maxsockets 10
```

**בעיות 11-15: טיפים נוספים**

11. **שגיאות SSL:** `npm config set strict-ssl false` (זמני!)
12. **אין מקום בדיסק:** פנו מקום ב-`~/.npm` cache
13. **גרסת Node ישנה:** עדכנו ל-v20+
14. **תווים מיוחדים בנתיב:** העבירו לנתיב פשוט (בלי רווחים, עברית, וכו')
15. **Windows Defender חוסם:** הוסיפו חריגה זמנית

---

## חלק 3: אינטגרציה ל-IDE

### Claude Code - הגדרה מפורטת

#### מבוא ל-Claude Code

Claude Code הוא IDE מבית Anthropic שמותאם במיוחד לעבודה עם Claude AI. האינטגרציה של BMAD איתו היא הכי חלקה.

#### מה המתקין יוצר

אחרי ההתקנה, תמצאו:

```
your-project/
└── .claude/
    └── commands/
        └── bmad/
            ├── core/
            │   ├── agents/
            │   │   └── bmad-master.md
            │   └── workflows/
            │       └── party-mode.md
            ├── bmm/
            │   ├── agents/
            │   │   ├── dev.md
            │   │   ├── architect.md
            │   │   └── ... (11 more)
            │   └── workflows/
            │       ├── workflow-init.md
            │       ├── prd.md
            │       └── ... (30+ more)
            ├── bmb/
            └── cis/
```

#### איך להשתמש ב-Slash Commands

**בסיסי:**
```
/bmad:bmm:agents:dev
```

**מבנה הפקודה:**
```
/bmad:{module}:{type}:{name}
```

- `module`: core, bmm, bmb, cis
- `type`: agents, workflows, tasks
- `name`: שם האג'נט/וורקפלואו

**דוגמאות:**
```
# טעינת אג'נטים
/bmad:bmm:agents:architect
/bmad:bmm:agents:pm
/bmad:core:agents:bmad-master

# הרצת וורקפלואים
/bmad:bmm:workflows:prd
/bmad:bmm:workflows:tech-spec
/bmad:bmm:workflows:dev-story

# וורקפלואים של CIS
/bmad:cis:workflows:brainstorming
/bmad:cis:workflows:design-thinking
```

#### Auto-complete ב-Claude Code

כשאתם מקלידים `/` - Claude Code מציג רשימה של כל הפקודות הזמינות. פשוט התחילו להקליד `bmad` והוא יסנן אוטומטית.

#### טיפים מתקדמים

**1. שילוב מספר אג'נטים:**
```
/bmad:bmm:agents:dev
# עכשיו בצ'אט:
"@architect, @dev - let's discuss this architecture decision"
```

**2. הרצת workflow ישירות:**
אתם יכולים להריץ workflow בלי לטעון אג'נט קודם - פשוט:
```
/bmad:bmm:workflows:dev-story
```

**3. Project Context (אופציונלי):**
אם בחרתם "Enable project context" בזמן ההתקנה, Claude יוכל לגשת לקבצים בפרויקט אוטומטית.

#### פתרון בעיות נפוצות ב-Claude Code

**בעיה: הפקודות לא מופיעות**

**פתרון:**
```bash
# בדקו שהקבצים נוצרו
ls .claude/commands/bmad/

# אם ריק - הריצו:
npx bmad-method build

# אחר כך רענון ב-Claude Code (Reload Window)
```

**בעיה: "Agent not loading"**

**פתרון:**
- סגרו ופתחו מחדש את Claude Code
- בדקו שהקובץ `.md` תקין (פתחו אותו ידנית)
- נסו slash command אחר

---

### Cursor - הגדרה מפורטת

#### מבוא ל-Cursor

Cursor הוא fork של VS Code עם יכולות AI מתקדמות. הוא משתמש ב-MDC (Manual Documentation Context) rules.

#### מה המתקין יוצר

```
your-project/
└── .cursor/
    └── rules/
        └── bmad/
            ├── index.md (רשימת כל האג'נטים)
            ├── core/
            │   └── agents/
            │       └── bmad-master.md
            ├── bmm/
            │   ├── agents/
            │   │   ├── dev.md
            │   │   └── ...
            │   └── workflows/
            │       └── ...
            ├── bmb/
            └── cis/
```

#### איך להשתמש ב-Rules

**Rules ב-Cursor הם Manual** - הם לא נטענים אוטומטית!

**שיטה 1: @-mention באג'נט ספציפי**

```
@bmad/bmm/agents/dev
```

בצ'אט אז תגידו:
```
Help me refactor this code
```

**שיטה 2: @-mention במודול שלם**

```
@bmad/bmm
```

זה יטען את כל המודול (כל האג'נטים והוורקפלואים) - **הזהירות! זה הרבה context**

**שיטה 3: @-mention בindex**

```
@bmad/index
```

זה מראה לכם רשימה של כל מה שזמין.

#### Best Practices ל-Cursor

**1. טענו רק מה שאתם צריכים:**
```
# טוב
@bmad/bmm/agents/dev

# לא טוב (יותר מדי context)
@bmad/bmm
```

**2. שלבו מספר אג'נטים במידת הצורך:**
```
@bmad/bmm/agents/architect
@bmad/bmm/agents/dev

Can you help me design and implement this feature?
```

**3. הסירו rules כשלא צריכים:**
כל rule שאתם ממנשנים נשאר ב-context - אז לא לשכוח להסיר!

#### Advanced: Custom Rules

אתם יכולים ליצור rules מותאמים אישית:

```markdown
<!-- .cursor/rules/my-custom-rule.md -->
# My Custom Development Rules

- Always use TypeScript
- Follow company coding standards
- Include tests for every feature
```

ואז:
```
@my-custom-rule
@bmad/bmm/agents/dev

Build a new user service
```

---

### Windsurf - הגדרה מפורטת

#### מבוא ל-Windsurf

Windsurf מטפל ב-workflows כישויות מובנות עם רמות אוטונומיה שונות.

#### מה המתקין יוצר

```
your-project/
└── .windsurf/
    └── workflows/
        ├── bmad-core-bmad-master.md (mode: 3)
        ├── bmad-bmm-dev.md (mode: 3)
        ├── workflow-init.md (mode: 2)
        └── ... (all agents & workflows)
```

#### Execution Modes

Windsurf משתמש ב-`auto_execution_mode`:

- **Mode 2:** Guided execution - המשתמש מאשר כל פעולה
- **Mode 3:** High autonomy - האג'נט פועל באופן עצמאי יותר

**אג'נטים = Mode 3** (אוטונומיה גבוהה)  
**Tasks/Workflows = Mode 2** (ביצוע מודרך)

#### איך להשתמש

**1. פתיחת Workflows Panel:**
- Windsurf Menu → Workflows
- או Command Palette: "Show Workflows"

**2. בחירת Workflow/Agent:**
רשימה של כל הworkflows תופיע. בחרו את מה שאתם צריכים:
- `bmad-bmm-dev` - טוען את ה-dev agent
- `workflow-init` - מריץ את workflow-init
- וכו'

**3. ביצוע:**
- Workflows (mode 2) ישאלו אישור לפני כל שלב
- Agents (mode 3) יפעלו באופן עצמאי יותר

#### Customization

ערכו את קבצי ה-workflow כדי לשנות execution mode:

```yaml
# .windsurf/workflows/my-agent.md
---
name: My Agent
auto_execution_mode: 2  # שנו ל-2 לאוטונומיה נמוכה יותר
---
```

---

### VS Code + GitHub Copilot

#### מבוא

VS Code עם GitHub Copilot תומך ב-"Chat Modes" שמאפשרים אג'נטים שונים.

#### מה המתקין יוצר

```
your-project/
├── .github/
│   └── chatmodes/
│       └── bmad/
│           ├── bmad-dev.md
│           ├── bmad-architect.md
│           └── ... (all agents)
└── .vscode/
    └── settings.json (עם הגדרות BMAD)
```

#### איך להשתמש

**1. פתיחת Chat:**
- לחצו על אייקון Copilot בסרגל הצד
- או `Ctrl+Shift+I` (Windows/Linux) / `Cmd+Shift+I` (Mac)

**2. בחירת Mode:**
- למעלה בצ'אט יש dropdown "Select Mode"
- לחצו עליו ובחרו `bmad-dev` (או אג'נט אחר)

**3. שיחה:**
עכשיו Copilot מתנהג כמו האג'נט שבחרתם!

#### הגדרות ב-settings.json

המתקין יוסיף:
```json
{
  "github.copilot.chat.maxRequests": 50,
  "github.copilot.advanced": {
    "autofix": "enabled"
  }
}
```

אתם יכולים לשנות את זה לפי הצורך.

---

### Cline, Roo, ו-IDEs נוספים

#### Cline - Toggleable Rules

**מיקום:**
```
.clinerules/
├── 01-core-dev.md
├── 02-bmm-architect.md
└── ...
```

**שימוש:**
1. פתחו Rules Panel (אייקון מתחת לצ'אט)
2. Toggle ON את האג'נט הרצוי
3. הקלידו `@dev` (או שם אחר) בצ'אט
4. Toggle OFF כשסיימתם

**חשוב:** Rules מכבות כברירת מחדל למניעת זיהום context!

#### Roo - Custom Modes

**מיקום:**
```
.roomodes/
├── bmad-dev.mode
├── bmad-architect.mode
└── ...
```

**שימוש:**
1. פתחו Mode Selector
2. בחרו `bmad-dev`
3. ה-mode פעיל עם הרשאות קבצים מוגדרות מראש

#### IDEs נוספים

BMAD תומך גם ב:
- **Auggie**
- **Codex**
- **Qwen**
- **Gemini Code Assist**
- **Kilo**
- **Trae**
- **OpenCode**
- **iFlow**

כל אחד מקבל את קבצי התצורה המתאימים לו אוטומטית.

---

### אינטגרציה מותאמת אישית

#### תרחיש: אתם משתמשים ב-IDE שלא ברשימה

אם ה-IDE שלכם לא נתמך מובנית, אתם יכולים ליצור אינטגרציה ידנית.

**שלב 1: הבינו מה ה-IDE צריך**

רוב ה-IDEs זקוקים לאחד מאלה:
- קבצי Markdown עם הוראות לאג'נטים
- קבצי JSON/YAML עם metadata
- מבנה תיקיות ספציפי

**שלב 2: צרו את המבנה**

```bash
# לדוגמה, ליצירת אינטגרציה ל-IDE בשם "MyIDE"
mkdir -p .myide/agents
mkdir -p .myide/workflows
```

**שלב 3: העתיקו את קבצי האג'נטים**

```bash
# מ-BMAD לתיקיית ה-IDE שלכם
cp -r bmad/bmm/agents/*.md .myide/agents/
cp -r bmad/bmm/workflows/*.md .myide/workflows/
```

**שלב 4: התאימו לפורמט של ה-IDE**

ערכו את הקבצים בהתאם לדרישות ה-IDE שלכם (metadata, structure, וכו').

#### פנייה לתמיכה

אם ה-IDE שלכם פופולרי, שקלו לפנות לצוות BMAD:
- Discord: https://discord.gg/gk8jAdXWmj
- GitHub Issues: הציעו IDE חדש

---

### שיטות טעינת אג'נטים

יש **3 שיטות עיקריות** לטעינת אג'נטים ב-BMAD, תלוי ב-IDE:

#### שיטה 1: Slash Commands (Claude Code, Windsurf)

```
/bmad:bmm:agents:dev
```

**יתרונות:**
- ✅ מהיר וקל
- ✅ גישה מיידית לכל האג'נטים
- ✅ לא צריך לפתוח קבצים ידנית

**חסרונות:**
- ❌ צריך לזכור את שמות הפקודות
- ❌ לא בכל ה-IDEs

**מתי להשתמש:**
- כשאתם יודעים בדיוק איזה אג'נט אתם רוצים
- בעבודה מהירה עם אג'נטים מוכרים

#### שיטה 2: Reference/Context (Cursor, Cline)

```
@bmad/bmm/agents/dev
```

**יתרונות:**
- ✅ שליטה מלאה על מה בcontext
- ✅ מניעת זיהום context
- ✅ אפשר לשלב מספר אג'נטים

**חסרונות:**
- ❌ ידני יותר
- ❌ צריך להוסיף ידנית בכל צ'אט

**מתי להשתמש:**
- כשאתם רוצים שליטה מדויקת על הcontext
- בעבודה עם מספר אג'נטים במקביל

#### שיטה 3: Mode/Workflow Selection (Windsurf, Roo, GitHub Copilot)

בחירה מתוך תפריט או dropdown:

**יתרונות:**
- ✅ ידידותי למתחילים
- ✅ אין צורך לזכור שמות
- ✅ UI ברור

**חסרונות:**
- ❌ יותר clicks
- ❌ לא תמיד מהיר

**מתי להשתמש:**
- כשאתם מתחילים עם BMAD
- כשאתם לא זוכרים את השמות המדויקים

---

### קונפיגורציית Slash Commands

Slash commands הם הדרך המהירה ביותר לגשת לאג'נטים ווורקפלואים ב-BMAD.

#### מבנה פקודות

**פורמט כללי:**
```
/bmad:{module}:{type}:{name}
```

**דוגמאות:**
```
/bmad:bmm:agents:dev              # אג'נט Dev מ-BMM
/bmad:bmm:workflows:prd           # וורקפלואו PRD
/bmad:core:agents:bmad-master     # BMad Master
/bmad:cis:workflows:brainstorming # וורקפלואו Brainstorming
```

#### רשימת Modules זמינים

| Module | תיאור | אג'נטים | וורקפלואים |
|--------|-------|----------|-------------|
| `core` | ליבה | bmad-master | party-mode, workflow-init |
| `bmm` | BMad Method | 12 אג'נטים | 34 וורקפלואים |
| `bmb` | BMad Builder | bmad-builder | 7 וורקפלואים |
| `cis` | Creative Intelligence | 5 אג'נטים | 5 וורקפלואים |

#### כיצד לגלות פקודות זמינות

**שיטה 1: Autocomplete**

ב-Claude Code או IDEs אחרים, פשוט התחילו להקליד:
```
/bmad:
```

ותראו רשימה של כל האפשרויות.

**שיטה 2: קריאת Manifest**

```bash
cat bmad/_cfg/agent-manifest.csv
cat bmad/_cfg/workflow-manifest.csv
```

**שיטה 3: שאלו את BMad Master**

טענו את bmad-master ושאלו:
```
*list-workflows
*list-tasks
```

#### התאמה אישית של Commands

אם אתם רוצים שמות פקודות קצרים יותר או שונים, אתם יכולים:

**אופציה 1: Aliases ב-IDE**

ב-Claude Code, ערכו `.claude/commands/`:
```yaml
# .claude/commands/shortcuts.yaml
- name: dev
  command: /bmad:bmm:agents:dev
- name: prd
  command: /bmad:bmm:workflows:prd
```

**אופציה 2: Shell Aliases (למתקדמים)**

```bash
# .bashrc או .zshrc
alias bmad-dev='echo "/bmad:bmm:agents:dev"'
```

---

## חלק 4: קונפיגורציה מתקדמת

### מבנה config.yaml

כל מודול ב-BMAD מכיל קובץ `config.yaml` עם הגדרות למודול.

#### מיקום קבצים

```
bmad/
├── core/
│   └── config.yaml          # הגדרות ליבה
├── bmm/
│   └── config.yaml          # הגדרות BMad Method
├── bmb/
│   └── config.yaml          # הגדרות BMad Builder
└── cis/
    └── config.yaml          # הגדרות CIS
```

#### מבנה config.yaml טיפוסי

```yaml
# Core Module Configuration
# Version: 6.0.0-alpha.5
# Date: 2025-11-04T16:59:31.777Z

user_name: BMad
communication_language: English
document_output_language: English
output_folder: '{project-root}/docs'
```

**שדות חובה:**
- `user_name`: השם שהאג'נטים ישתמשו בו כדי לפנות אליכם
- `communication_language`: שפת התקשורת עם האג'נטים
- `document_output_language`: שפת המסמכים שנוצרים
- `output_folder`: תיקייה לשמירת מסמכים

---

### הגדרות משתמש

#### user_name

**מה זה:** השם שהאג'נטים יפנו אליכם בו בשיחות.

**דוגמאות:**
```yaml
user_name: David
user_name: Sarah
user_name: Team Alpha
```

**טיפ:** השתמשו בשם מקצועי. האג'נטים ישתמשו בו בכל אינטראקציה.

#### כיצד לשנות

**אופציה 1: עריכה ידנית**

```bash
# ערכו את הקובץ
nano bmad/core/config.yaml

# שנו את השורה:
user_name: YourNewName
```

**אופציה 2: הרצת התקנה מחדש (Quick Update)**

```bash
npx bmad-method@alpha install
```

בחרו "Quick Update" כשתתבקשו, והמתקין ישאל רק על שדות חדשים.

---

### הגדרות שפה (תקשורת מול פלט)

BMAD v6 תומך ב**2 הגדרות שפה נפרדות**:

#### 1. communication_language

**מה זה:** השפה שהאג'נטים ישתמשו בה כדי לדבר איתכם.

**ערכים נתמכים:**
```yaml
communication_language: English
communication_language: Hebrew
communication_language: Spanish
communication_language: French
# ... או כל שפה שה-LLM תומך בה
```

**דוגמה:**
```yaml
communication_language: Hebrew
```

כעת האג'נטים ידברו איתכם בעברית:
```
🧙 BMad Master מתעורר

שלום David! BMad Master מוכן לשרת אותך...
```

#### 2. document_output_language

**מה זה:** השפה שבה מסמכים (PRD, Architecture, Stories) נכתבים.

**למה זה נפרד?**

לעיתים אתם רוצים:
- לדבר עם האג'נט בעברית
- אבל המסמכים יהיו באנגלית (לצוות בינלאומי)

**דוגמה:**
```yaml
communication_language: Hebrew
document_output_language: English
```

**תוצאה:**
- השיחות עם האג'נט בעברית
- PRD, Tech Spec, וכו' נכתבים באנגלית

#### תרחישי שימוש נפוצים

**תרחיש 1: צוות ישראלי**
```yaml
communication_language: Hebrew
document_output_language: Hebrew
```

**תרחיש 2: צוות מעורב**
```yaml
communication_language: Hebrew
document_output_language: English
```

**תרחיש 3: צוות בינלאומי**
```yaml
communication_language: English
document_output_language: English
```

---

### קונפיגורציית תיקיות פלט

#### output_folder

**מה זה:** המיקום שבו BMAD שומר את כל המסמכים שנוצרו.

**ערך ברירת מחדל:**
```yaml
output_folder: '{project-root}/docs'
```

**{project-root}** הוא placeholder שמתורגם אוטומטית לשורש הפרויקט.

#### דוגמאות התאמה אישית

**דוגמה 1: תיקייה מותאמת אישית**
```yaml
output_folder: '{project-root}/documentation'
```

**דוגמה 2: תיקייה מחוץ לפרויקט**
```yaml
output_folder: '/Users/david/Documents/MyProject'
```

**דוגמה 3: תיקייה בענן (OneDrive/Dropbox)**
```yaml
output_folder: '/Users/david/OneDrive/Projects/MyProject/docs'
```

#### מבנה תיקיות אוטומטי

BMAD יוצר אוטומטית תיקיות משנה:

```
docs/
├── prd/
│   └── product-requirements.md
├── architecture/
│   └── technical-architecture.md
├── stories/
│   ├── story-001.md
│   ├── story-002.md
│   └── ...
├── test-plans/
│   └── test-strategy.md
└── game-design/
    └── game-design-document.md
```

#### שיקולים

**שימוש ב-Version Control:**

אם אתם משתמשים ב-Git, שימו את output_folder **בתוך הפרויקט**:
```yaml
output_folder: '{project-root}/docs'
```

כך המסמכים יהיו tracked ב-Git.

**צוותים:**

כל חברי הצוות צריכים להשתמש ב**אותו output_folder יחסי**:
```yaml
# כולם משתמשים ב:
output_folder: '{project-root}/docs'

# ולא:
output_folder: '/Users/david/my-docs'  # רע! ייחודי למשתמש
```

---

### קונפיגורציה ספציפית למודול

כל מודול יכול להוסיף שדות קונפיגורציה משלו.

#### BMM (BMad Method)

```yaml
# bmad/bmm/config.yaml
user_name: David
communication_language: English
document_output_language: English
output_folder: '{project-root}/docs'

# הגדרות ייחודיות ל-BMM
enable_game_development: true
default_planning_track: bmad_method
```

**שדות ייחודיים:**

- `enable_game_development`: האם להציג אג'נטים ווורקפלואים של פיתוח משחקים
- `default_planning_track`: מסלול ברירת מחדל (quick_flow, bmad_method, enterprise)

#### CIS (Creative Intelligence Suite)

```yaml
# bmad/cis/config.yaml
user_name: David
communication_language: English
document_output_language: English
output_folder: '{project-root}/docs'

# הגדרות ייחודיות ל-CIS
default_facilitation_style: energetic
brainstorming_techniques_limit: 10
```

**שדות ייחודיים:**

- `default_facilitation_style`: סגנון ברירת מחדל (energetic, calm, analytical)
- `brainstorming_techniques_limit`: מספר טכניקות מקסימלי לסשן

#### BMB (BMad Builder)

```yaml
# bmad/bmb/config.yaml
user_name: David
communication_language: English
document_output_language: English
output_folder: '{project-root}/docs'

# הגדרות ייחודיות ל-BMB
custom_agents_folder: '{project-root}/my-agents'
workflow_template_style: detailed
```

---

### קבצי התאמה אישית של אג'נטים

אחת מהתכונות החזקות ביותר של BMAD v6 היא **התאמה אישית של אג'נטים בלי לגעת בקבצים המקוריים**.

#### מיקום קבצי Customize

```
bmad/_cfg/agents/
├── core-bmad-master.customize.yaml
├── bmm-dev.customize.yaml
├── bmm-architect.customize.yaml
├── bmm-pm.customize.yaml
└── ...
```

**יש קובץ customize לכל אג'נט!**

#### מבנה קובץ Customize

```yaml
# Agent Customization
# Customize any section below - all are optional
# After editing: npx bmad-method build <agent-name>

# Override agent name
agent:
  metadata:
    name: ""

# Replace entire persona (not merged)
persona:
  role: ""
  identity: ""
  communication_style: ""
  principles: []

# Add custom critical actions (appended after standard config loading)
critical_actions: []

# Add persistent memories for the agent
memories: []
# Example:
# memories:
#   - "User prefers detailed technical explanations"
#   - "Current project uses React and TypeScript"

# Add custom menu items (appended to base menu)
# Don't include * prefix or help/exit - auto-injected
menu: []
# Example:
# menu:
#   - trigger: my-workflow
#     workflow: "{project-root}/custom/my.yaml"
#     description: My custom workflow

# Add custom prompts (for action="#id" handlers)
prompts: []
# Example:
# prompts:
# - id: my-prompt
#   content: |
#     Prompt instructions here
```

#### דוגמה 1: שינוי שם אג'נט

**קובץ:** `bmad/_cfg/agents/bmm-dev.customize.yaml`

```yaml
agent:
  metadata:
    name: "CodeMaster Pro"
```

**תוצאה:** האג'נט יזדהה כ-"CodeMaster Pro" במקום "Amelia".

#### דוגמה 2: שינוי אישיות

```yaml
persona:
  role: "Senior Full-Stack Developer + Mentor"
  identity: "Expert developer with 15 years experience, patient teacher"
  communication_style: "Friendly, encouraging, uses emojis"
  principles:
    - "Always explain why, not just how"
    - "Encourage best practices"
    - "Patient with juniors"
```

#### דוגמה 3: הוספת זיכרונות

```yaml
memories:
  - "Team uses React 18 with TypeScript"
  - "Project follows Airbnb style guide"
  - "User prefers functional components over class components"
  - "Always use async/await instead of .then()"
```

**איך זה עובד:**

האג'נט "זוכר" את המידע הזה בכל שיחה ומתאים את התשובות בהתאם.

#### דוגמה 4: הוספת תפריט מותאם אישית

```yaml
menu:
  - trigger: code-review
    workflow: "{project-root}/custom-workflows/code-review.yaml"
    description: "Run my custom code review workflow"
  - trigger: refactor-check
    workflow: "{project-root}/custom-workflows/refactor.yaml"
    description: "Check if refactoring is needed"
```

#### איך להחיל שינויים

**אופציה 1: Build מחדש (מומלץ)**

```bash
npx bmad-method build bmm-dev
```

זה יבנה מחדש את האג'נט עם השינויים.

**אופציה 2: Reinstall**

```bash
npx bmad-method@alpha install
```

בחרו "Quick Update" - זה יחיל את כל השינויים.

**אופציה 3: עריכה ידנית (מתקדמים בלבד)**

שינוי ישיר של `bmad/bmm/agents/dev.md` - **לא מומלץ** כי עדכונים ידרסו את השינויים!

---

### יצירת אג'נט מותאם אישית - יסודות

לפעמים אתם רוצים אג'נט חדש לגמרי. הנה המבוא.

#### מתי ליצור אג'נט מותאם אישית?

**תרחישים טובים:**
- יש לכם תחום מומחיות ספציפי (DevOps, Security, Data Science)
- אתם רוצים אג'נט לתהליך ייחודי לארגון שלכם
- צריך אג'נט בשפה/סגנון ספציפי

**אל תיצרו אג'נט אם:**
- אתם יכולים להשתמש באג'נט קיים + customize
- זה רק שינוי קטן לאג'נט קיים

#### יצירה בסיסית (3 צעדים)

**צעד 1: צרו קובץ Markdown**

```bash
touch bmad/bmm/agents/my-agent.md
```

**צעד 2: הוסיפו frontmatter**

```markdown
---
name: "My Agent"
description: "What this agent does"
---
```

**צעד 3: הוסיפו תוכן**

```markdown
You are My Agent, an expert in [domain].

Your role is to help users with [specific tasks].

When users ask you to [X], you should [Y].
```

#### דוגמה מלאה: אג'נט DevOps

```markdown
---
name: "DevOps Engineer"
description: "Infrastructure and deployment expert"
---

You are a DevOps Engineer with 10 years of experience in:
- Cloud infrastructure (AWS, Azure, GCP)
- CI/CD pipelines
- Container orchestration (Kubernetes, Docker)
- Infrastructure as Code (Terraform, CloudFormation)

Your role is to help with:
1. Designing scalable infrastructure
2. Setting up CI/CD pipelines
3. Troubleshooting deployment issues
4. Optimizing cloud costs

Always follow these principles:
- Infrastructure as Code
- Security first
- Automation over manual work
- Monitor everything
```

#### רישום האג'נט במערכת

אחרי יצירה, הריצו:

```bash
npx bmad-method build
```

זה יוסיף את האג'נט החדש ל-manifest ויעשה אותו זמין ב-IDE.

**מידע נוסף:**

למדריך מקיף על יצירת אג'נטים, ראו:
- [BMB Module Documentation](../../src/modules/bmb/README.md)
- [Create Agent Workflow](../../src/modules/bmb/workflows/create-agent/README.md)

---

## חלק 5: נושאים מתקדמים

### הגדרת פרויקטים מרובים

אם אתם עובדים על **מספר פרויקטים** עם BMAD, יש כמה אסטרטגיות.

#### אסטרטגיה 1: התקנה נפרדת לכל פרויקט

**מבנה:**
```
~/Projects/
├── project-a/
│   ├── bmad/
│   ├── src/
│   └── docs/
└── project-b/
    ├── bmad/
    ├── src/
    └── docs/
```

**יתרונות:**
- ✅ בידוד מלא בין פרויקטים
- ✅ קונפיגורציה שונה לכל פרויקט
- ✅ גרסאות BMAD שונות אם צריך

**חסרונות:**
- ❌ תופס יותר מקום (~50MB לכל התקנה)
- ❌ צריך לעדכן כל אחד בנפרד

**מתי להשתמש:**
- פרויקטים לא קשורים
- צוותים שונים
- דרישות שונות

#### אסטרטגיה 2: Symlink לתיקיית BMAD משותפת (מתקדמים)

**מבנה:**
```
~/BMAD-Shared/
└── bmad/     # התקנה מרכזית

~/Projects/
├── project-a/
│   ├── bmad -> ~/BMAD-Shared/bmad
│   └── src/
└── project-b/
    ├── bmad -> ~/BMAD-Shared/bmad
    └── src/
```

**יצירת Symlink:**

```bash
# Linux/Mac
ln -s ~/BMAD-Shared/bmad ~/Projects/project-a/bmad

# Windows (PowerShell as Admin)
New-Item -ItemType SymbolicLink -Path "C:\Projects\project-a\bmad" -Target "C:\BMAD-Shared\bmad"
```

**יתרונות:**
- ✅ חוסך מקום דיסק
- ✅ עדכון אחד לכולם

**חסרונות:**
- ❌ קונפיגורציה משותפת
- ❌ מורכב יותר לניהול
- ❌ עלול ליצור בעיות ב-Git

**מתי להשתמש:**
- פרויקטים קטנים/אישיים
- קונפיגורציה זהה לכולם

#### אסטרטגיה 3: Monorepo

**מבנה:**
```
~/monorepo/
├── bmad/           # התקנה אחת
├── packages/
│   ├── app-a/
│   └── app-b/
└── docs/
    ├── app-a/
    └── app-b/
```

כל האפליקציות חולקות את אותו BMAD אבל יש להן `docs/` נפרד.

**יתרונות:**
- ✅ ניהול מרכזי
- ✅ שיתוף קוד ומסמכים
- ✅ עקביות בצוות

**מתי להשתמש:**
- פרויקטים קשורים
- צוות אחד

---

### סנכרון קונפיגורציה של צוות

כשצוות עובד על אותו פרויקט, חשוב לסנכרן הגדרות.

#### מה לסנכרן

**צריך לסנכרן (commit ל-Git):**
- ✅ `bmad/_cfg/agents/*.customize.yaml` - אג'נטים מותאמים
- ✅ `output_folder` configuration (יחסי, לא מוחלט!)
- ✅ `document_output_language` - כולם כותבים באותה שפה
- ✅ הגדרות ספציפיות למודול (game dev, וכו')

**לא צריך לסנכרן (הוסיפו ל-.gitignore):**
- ❌ `user_name` - ייחודי לכל משתמש
- ❌ `communication_language` - כל אחד יכול לבחור
- ❌ Paths מוחלטים ל-IDE

#### .gitignore מומלץ

```gitignore
# BMAD - Exclude personal settings, keep team settings
bmad/core/config.yaml
bmad/bmm/config.yaml
bmad/bmb/config.yaml
bmad/cis/config.yaml

# But include customizations (team-level)
!bmad/_cfg/

# IDE-specific (usually auto-generated)
.claude/
.cursor/
.windsurf/
.github/copilot/
.clinerules/
.roomodes/

# Keep IDE customizations if your team agrees
# !.claude/
# !.cursor/
```

#### תרחיש: חבר צוות חדש

**מה שהוא צריך לעשות:**

1. **Clone Repository**
```bash
git clone <repo-url>
cd <project>
```

2. **הריצו התקנה**
```bash
npx bmad-method@alpha install
```

3. **בחרו הגדרות אישיות**
   - `user_name`: השם שלהם
   - `communication_language`: השפה המועדפת שלהם
   - `document_output_language`: **צריך להיות זהה לצוות** (בדרך כלל English)

4. **כל ההתאמות של הצוות נטענות אוטומטית**
   - אג'נטים מותאמים אישית מ-`_cfg/`
   - workflow paths
   - הגדרות מודול

---

### שיטות עבודה מומלצות ב-Version Control

#### מבנה Git מומלץ

```
your-repo/
├── .gitignore
├── bmad/
│   ├── _cfg/              # Commit - התאמות צוות
│   ├── core/config.yaml   # Ignore - הגדרות אישיות
│   └── bmm/config.yaml    # Ignore - הגדרות אישיות
├── docs/                  # Commit - מסמכי פרויקט
│   ├── prd/
│   ├── architecture/
│   └── stories/
└── src/                   # Commit - קוד
```

#### Branching Strategy עם BMAD

**Feature Branch:**
```bash
git checkout -b feature/user-auth

# עבדו עם BMAD:
/bmad:bmm:workflows:dev-story

# BMAD יוצר קבצים בdocs/stories/
git add docs/stories/story-042-user-auth.md
git commit -m "Add user authentication story"

# פיתוח
git add src/auth/
git commit -m "Implement user authentication"

git push origin feature/user-auth
```

**Pull Request:**
- הכללו את story files מ-docs/
- Code reviewers יכולים לראות גם את הסטורי
- אישור = מיזוג story + code ביחד

#### Document Sharding ו-Git

אם אתם משתמשים ב-[Document Sharding](../../docs/document-sharding-guide.md):

```
docs/
├── prd/
│   ├── _master.md         # Commit - מסמך ראשי
│   └── _shards/           # Commit - כל הshards
│       ├── overview.md
│       ├── features.md
│       └── requirements.md
```

**כולם ב-Git!** Sharding זה רק דרך לארגן, לא סיבה להוציא מ-Git.

---

### המלצות .gitignore

הנה `.gitignore` מומלץ לפרויקט BMAD:

```gitignore
# ====================
# BMAD Configuration
# ====================

# Personal settings (each team member has their own)
bmad/core/config.yaml
bmad/*/config.yaml

# Keep team-level customizations
!bmad/_cfg/

# ====================
# IDE Integration Files
# ====================

# Claude Code
.claude/

# Cursor
.cursor/

# Windsurf  
.windsurf/

# VS Code
.vscode/
!.vscode/settings.json  # Optional: commit if team shares settings

# Cline
.clinerules/

# Roo
.roomodes/

# GitHub Copilot
.github/copilot/

# ====================
# BMAD Outputs (Optional)
# ====================

# If you want to exclude generated docs (not recommended for most teams)
# docs/

# If you want to commit docs but exclude temp files
docs/**/*.tmp
docs/**/*.backup

# ====================
# Node.js / NPM
# ====================

node_modules/
npm-debug.log
yarn-error.log
package-lock.json  # or yarn.lock - pick one

# ====================
# OS Files
# ====================

.DS_Store
Thumbs.db
*.swp
*.swo
*~

# ====================
# BMAD Backups
# ====================

v4-backup/  # Created during v4->v6 upgrade
*.bak
```

#### מתי לא לignore את docs/

**Commit את docs/ אם:**
- ✅ צוות עובד ביחד על PRD/Architecture
- ✅ אתם רוצים history של שינויים במסמכים
- ✅ Code reviews כוללים גם סטוריז
- ✅ CI/CD צריך גישה למסמכים

**Ignore את docs/ אם:**
- ❌ מסמכים מאוד גדולים (>100MB)
- ❌ מסמכים רגישים (secrets, credentials)
- ❌ מסמכים שמתעדכנים כל הזמן ולא רלוונטיים לVCS

**המלצתנו:** Commit את docs/ - זה חלק מהפרויקט!

---

### אינטגרציה עם CI/CD

BMAD יכול להשתלב ב-CI/CD pipelines שלכם.

#### תרחישים נפוצים

**1. Validation של Stories**

ודאו שכל PR כולל story file:

```yaml
# .github/workflows/validate-pr.yml
name: Validate PR

on: [pull_request]

jobs:
  check-story:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Check for story file
        run: |
          if [ -z "$(git diff --name-only origin/main | grep 'docs/stories/')" ]; then
            echo "Error: No story file found in this PR"
            exit 1
          fi
```

**2. Auto-generation של Documentation**

יצירה אוטומטית של docs מקוד:

```yaml
# .github/workflows/docs.yml
name: Generate Docs

on:
  push:
    branches: [main]

jobs:
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      
      - name: Install BMAD
        run: npx bmad-method@alpha install --non-interactive
      
      - name: Generate docs
        run: |
          # הריצו workflow מותאם אישית שלכם
          # לדוגמה: npx bmad-method run workflow document-api
```

**3. Linting של BMAD Files**

```yaml
# .github/workflows/lint-bmad.yml
name: Lint BMAD

on: [pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Check BMAD structure
        run: |
          # בדיקה שמבנה התיקיות תקין
          if [ ! -d "bmad/_cfg" ]; then
            echo "Error: bmad/_cfg missing"
            exit 1
          fi
          
          # בדיקה שconfig files תקינים
          for config in bmad/*/config.yaml; do
            if [ -f "$config" ]; then
              echo "Validating $config"
              # Add YAML validation here
            fi
          done
```

#### Secrets בCI/CD

אם יש לכם מידע רגיש בconfig files:

**אל תשימו בGit!** השתמשו ב-GitHub Secrets או equivalent:

```yaml
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Create config
        env:
          API_KEY: ${{ secrets.API_KEY }}
        run: |
          echo "api_key: $API_KEY" > bmad/custom/config.yaml
```

---

## 🎓 סיכום וצעדים הבאים

### מה למדנו

✅ **התקנה מתקדמת** - כל התרחישים האפשריים  
✅ **אינטגרציה ל-IDE** - כל ה-IDEs הפופולריים  
✅ **קונפיגורציה מלאה** - config.yaml, customize, שפות  
✅ **אג'נטים מותאמים אישית** - יצירה ועריכה  
✅ **עבודת צוות** - Git, CI/CD, סנכרון  

### המלצות מרכזיות

1. **התקנה ראשונית:** התחילו עם ברירות מחדל
2. **אינטגרציה ל-IDE:** Claude Code או Cursor מומלצים
3. **קונפיגורציה:** התאימו אישית רק מה שצריך
4. **צוותים:** השתמשו ב-Git ל-team settings, ignore personal settings
5. **CI/CD:** הוסיפו validations בהדרגה

### מה הלאה?

**מדריכים קשורים:**
- [התחלה מהירה](./00-התחלה-מהירה-לכולם.md) - אם עוד לא קראתם
- [מילון מונחים](./01-מילון-מונחים-מלא.md) - הסבר על כל המונחים
- [מדריך וורקפלואים](./03-workflows-part-1a-intro-critical.md) - איך להשתמש בBMAD
- [שיטות עבודה מומלצות](./05-שיטות-עבודה-מומלצות.md) - Best practices (בקרוב)
- [שאלות ותשובות - התקנה](./06-faq-part-1b-installation.md) - פתרון בעיות נפוצות

**משאבים נוספים:**
- [Discord Community](https://discord.gg/gk8jAdXWmj) - קבלו עזרה מהקהילה
- [GitHub Issues](https://github.com/bmad-code-org/BMAD-METHOD/issues) - דווחו על בעיות
- [Video Tutorials](https://www.youtube.com/@BMadCode) - מדריכי וידאו

---

<div dir="rtl" align="center">

**🎉 מזל טוב! השלמתם את המדריך להתקנה והגדרה מתקדמת!**

עכשיו אתם מומחים בהתקנת וקונפיגורציה של BMAD.  
זמן לבנות משהו מדהים! 🚀

---

*נוצר עבור BMAD-METHOD v6 | עדכון אחרון: נובמבר 2024*

[🏠 חזרה לדף הבית](./README.md) | [📚 כל המדריכים](./README.md#-מדריכים-זמינים)

</div>